# 3dtoday - Система диагностики проблем 3D-печати

## 🎯 Описание проекта

Система диагностики проблем 3D-печати с использованием RAG (Retrieval Augmented Generation) и мультимодального анализа изображений дефектов.

## 📋 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `config.env.example` в `config.env` и заполните необходимые параметры:

```bash
cp config.env.example config.env
# Отредактируйте config.env
```

### 3. Запуск Qdrant (векторная БД)

**Вариант 1: Через скрипт (рекомендуется)**
```bash
./scripts/start_qdrant.sh
```

**Вариант 2: Через docker-compose**
```bash
docker-compose up -d qdrant
# или
docker compose up -d qdrant
```

**Вариант 3: Прямой запуск Docker**
```bash
docker run -d -p 6333:6333 --name qdrant_3dtoday qdrant/qdrant
```

**Проверка статуса:**
```bash
./scripts/check_services.sh
```

### 4. Запуск Ollama (если не запущен)

```bash
ollama serve
```

Или используйте утилиту из проекта:
```python
from backend.app.utils.ollama_manager import ensure_ollama_running
ensure_ollama_running(start_if_not=True)
```

### 5. Запуск FastAPI сервера

```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 6. Запуск Streamlit интерфейса

```bash
cd frontend
streamlit run streamlit_app.py
```

## 📁 Структура проекта

```
3dtoday/
├── backend/
│   ├── app/
│   │   ├── mcp/              # MCP сервер и клиент
│   │   ├── agents/           # Агенты (KB Curator, Vision, Diagnostic)
│   │   ├── services/         # Сервисы (RAG, Vector DB, LLM)
│   │   ├── models/           # Pydantic модели
│   │   └── main.py           # FastAPI приложение
│   └── requirements.txt
├── frontend/
│   └── streamlit_app.py      # Streamlit интерфейс
├── knowledge_base/
│   ├── articles/             # Статьи в JSON формате
│   └── examples/             # Примеры Q/A пар
├── scripts/                  # Скрипты для управления сервисами
├── config.env                # Конфигурация (не коммитится)
└── README.md
```

## 🔧 Конфигурация

Основные настройки в `config.env`:

- **LLM_PROVIDER**: `ollama` (локально) или `openai` (ProxyAPI)
- **QDRANT_HOST**: хост Qdrant (по умолчанию `localhost`)
- **QDRANT_PORT**: порт Qdrant (по умолчанию `6333`)
- **OLLAMA_BASE_URL**: URL Ollama (по умолчанию `http://localhost:11434`)

## 📚 Документация

- `IMPLEMENTATION_STEPS.md` - пошаговый план реализации
- `MCP_AGENT_APPROACH.md` - подход с MCP Agent
- `KB_CURATOR_AGENT.md` - агент-куратор KB
- `KB_MAINTENANCE.md` - руководство по поддержке KB
- `REALISTIC_MVP_PLAN.md` - реалистичный план на 2 недели

## 🚀 Текущий статус

**ШАГ 1:** ✅ Настройка проекта и структуры (завершен)
**ШАГ 2:** 🔄 Базовая инфраструктура (в процессе)

**Следующий шаг:** ШАГ 3 - KB MCP Server

## 🔗 GitHub

Репозиторий: https://github.com/kobyzev-yuri/3dtoday.git

## 📝 Лицензия

Проект для внутреннего использования.
