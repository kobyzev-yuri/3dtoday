# 3dtoday - Система диагностики проблем 3D-печати

## 🎯 Описание проекта

Система диагностики проблем 3D-печати с использованием RAG (Retrieval Augmented Generation) и мультимодального анализа изображений дефектов.

## 📋 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `config.env.example` в `config.env` и заполните необходимые параметры:

```bash
cp config.env.example config.env
# Отредактируйте config.env
```

### 3. Запуск Qdrant (векторная БД)

**Вариант 1: Через скрипт (рекомендуется)**
```bash
./scripts/start_qdrant.sh
```

**Вариант 2: Через docker-compose**
```bash
docker-compose up -d qdrant
# или
docker compose up -d qdrant
```

**Вариант 3: Прямой запуск Docker**
```bash
docker run -d -p 6333:6333 --name qdrant_3dtoday qdrant/qdrant
```

**Проверка статуса:**
```bash
./scripts/check_services.sh
```

### 4. Запуск Ollama (если не запущен)

```bash
ollama serve
```

Или используйте утилиту из проекта:
```python
from backend.app.utils.ollama_manager import ensure_ollama_running
ensure_ollama_running(start_if_not=True)
```

### 5. Запуск FastAPI сервера

```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 6. Запуск Streamlit интерфейса

```bash
cd frontend
streamlit run streamlit_app.py
```

## 📁 Структура проекта

```
3dtoday/
├── backend/
│   ├── app/
│   │   ├── mcp/              # MCP сервер и клиент
│   │   ├── agents/           # Агенты (KB Curator, Vision, Diagnostic)
│   │   ├── services/         # Сервисы (RAG, Vector DB, LLM)
│   │   ├── models/           # Pydantic модели
│   │   └── main.py           # FastAPI приложение
│   └── requirements.txt
├── frontend/
│   └── streamlit_app.py      # Streamlit интерфейс
├── knowledge_base/
│   ├── articles/             # Статьи в JSON формате
│   └── examples/             # Примеры Q/A пар
├── scripts/                  # Скрипты для управления сервисами
├── config.env                # Конфигурация (не коммитится)
└── README.md
```

## 🔧 Конфигурация

Основные настройки в `config.env`:

- **LLM_PROVIDER**: `ollama` (локально), `openai` (ProxyAPI) или `gemini` (ProxyAPI)
  - Система автоматически переключается на доступный провайдер при недоступности основного
  - Порядок fallback: ollama → gemini → openai (или в зависимости от настроек)
- **QDRANT_HOST**: хост Qdrant (по умолчанию `localhost`)
- **QDRANT_PORT**: порт Qdrant (по умолчанию `6333`)
- **QDRANT_COLLECTION**: коллекция для текстовых эмбеддингов (по умолчанию `kb_3dtoday`)
- **QDRANT_IMAGE_COLLECTION**: коллекция для изображений (по умолчанию `kb_3dtoday_images`)
- **EMBEDDING_DIMENSION**: размерность текстовых эмбеддингов (по умолчанию `768`)
- **IMAGE_EMBEDDING_DIMENSION**: размерность эмбеддингов изображений (по умолчанию `512`)
- **OPENCLIP_MODEL**: модель OpenCLIP (по умолчанию `ViT-B-16`)
- **OPENCLIP_PRETRAINED**: претрейнинг OpenCLIP (по умолчанию `openai`)
- **OLLAMA_BASE_URL**: URL Ollama (по умолчанию `http://localhost:11434`)

Подробнее: см. `docs/CONFIGURATION.md`

## 📚 Документация

### Основные руководства

- **[Установка и запуск](docs/INSTALLATION.md)** — как установить и запустить систему
- **[Архитектура системы](docs/ARCHITECTURE.md)** — архитектура агентов и dataflow диаграммы
- **[Управление KB](docs/KB_MANAGEMENT.md)** — как наполнять базу знаний (для администраторов)
- **[Руководство пользователя](docs/USER_GUIDE.md)** — как использовать систему диагностики
- **[Конфигурация](docs/CONFIGURATION.md)** — описание параметров конфигурации

### Дополнительная документация

- `docs/TROUBLESHOOTING.md` — решение проблем
- `docs/ARTICLE_COLLECTION_PROCESS.md` — процесс сбора статей
- `docs/STREAMLIT_INTERFACES_GUIDE.md` — подробное руководство по интерфейсам
- `docs/DIALOG_SYSTEM.md` — система диалога с пользователем
- `docs/RELEVANCE_CHECKING.md` — проверка релевантности статей

### Архив

Устаревшая документация перенесена в папку `archive/`:
- `archive/STATUS.md` — устаревший статус проекта
- `archive/IMPLEMENTATION_STEPS.md` — устаревший план реализации
- И другие устаревшие документы

## ✅ Текущий статус

**Реализовано:**
- ✅ Базовая инфраструктура (LLM клиент, Vector DB, эмбеддинги)
- ✅ Административный интерфейс для управления KB
- ✅ Пользовательский интерфейс для диагностики
- ✅ Автоматический fallback на LLM провайдеры
- ✅ Система диалога с уточняющими вопросами
- ✅ Индексация статей в векторную БД

**В разработке:**
- 🔄 Vision Agent для анализа изображений дефектов
- 🔄 Расширение базы знаний

## 🔗 GitHub

Репозиторий: https://github.com/kobyzev-yuri/3dtoday.git

## 📝 Лицензия

Проект для внутреннего использования.
