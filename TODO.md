1. Целевая предметная область
Из структуры 3dtoday.ru выбираем релевантные подразделы:

Техничка — основной источник по настройке и устранению неполадок
3D-печать — общие принципы, методики
3d-Оборудование → 3D-принтеры — особенности моделей
Расходные материалы — поведение PLA, PETG, ABS и др.
Применение — реальные кейсы с проблемами и решениями
Личные дневники — опыт пользователей, неочевидные баги и лайфхаки
Остальное (Акции, Песочница, Частные объявления и т.п.) — игнорируем.

2. Типы проблем, которые система должна распознавать
Система должна уметь классифицировать проблему по симптомам:

Дефекты печати: отслоение слоёв, «сопли», трещины, недозаполнение, «волосы», warping
Механические сбои: звук скрежета, залипание каретки, пропуск шагов
Проблемы с материалом: засор сопла, хрупкость, плохая адгезия, запах
Проблемы с ПО: сбой в слайсинге, неправильный G-code, остановка печати
Настройка/калибровка: неровный стол, неправильная высота первого слоя, оффсеты
3. Диалоговый цикл (интерактивная диагностика)
Шаг 1. Идентификация симптома
Пользователь: «У меня печать отслаивается»
→ Система:

«Что именно отслаивается?
— Нижние слои не держатся на столе?
— Верхние слои трескаются или отходят друг от друга?
— Или слои "расслаиваются" при изгибе детали?»

→ Это уточняющий вопрос по симптоматике. Ответ определяет направление.

Шаг 2. Сбор контекста
В зависимости от ответа — задаются технические уточнения:

«Какой у вас принтер? (например, Anycubic Kobra, Creality Ender-3)»
«Какой пластик используете? (PLA, PETG, ABS?)»
«Какая температура стола и сопла?»
«Используете ли вы enclosure / вентилятор / клей / PEI?»
→ Эти данные фильтруют релевантные статьи в RAG: например, только про PETG на Ender-3.

Шаг 3. Генерация рекомендации на основе контекста
Система ищет в базе знаний:

Статьи с таким же симптомом + таким же оборудованием + таким же материалом
Проверенные диагностические чек-листы
Конкретные значения параметров (например: «для PETG на Ender-3: стол 80°C, сопло 235°C, вентилятор 30%»)
→ Ответ содержит:

Возможные причины (не совпадают, а с вероятностью: «Скорее всего — недостаточная температура стола»)
Шаги по диагностике: «Проверьте, не отгибается ли угол детали сразу после печати первого слоя»
Конкретные настройки: «Увеличьте bed temperature до 80°C, отключите вентилятор первых 3 слоёв»
Ссылки: «Подробнее: Как настроить адгезию при печати PETG»
Шаг 4. Итерация (если решение не сработало)
Пользователь: «Повысил температуру — всё равно отслаивается»
→ Система:

«Попробуем другой путь.
— Стол чистый? (масло от пальцев ухудшает адгезию)
— Используете ли вы skirt/brim?
— Есть ли признаки перегрева сопла (дым, запах)?»

→ Это адаптивный диалог, а не статичная база знаний.

4. Как это работает внутри (логически)
RAG работает не по исходному запросу, а по структуре проблемы:
(симптом + принтер + материал + параметры) → релевантные фрагменты
База знаний должна быть размечена не просто по категориям, а по:
Типу проблемы
Оборудованию
Материалу
Этапу печати (первый слой, заполнение, постобработка)
Модель (Ollama или Gemini) используется не для "ответа", а для:
Формулировки уточняющих вопросов
Синтеза информации из нескольких источников
Преобразования технических данных в читаемые рекомендации
5. Итоговая ценность для пользователя
Он получает не статью, а диагностическую сессию, как от опытного инженера:

Система понимает контекст
Адаптируется под его оборудование
Предлагает действия, а не теорию
Ссылается на авторитетные источники (реальные статьи с 3dtoday.ru)
Если вы согласны с такой моделью — следующим шагом может быть:

Формализация диагностических деревьев для частых проблем (например, «отслоение» → 3 ветки: стол, материал, охлаждение)
Проектирование метаданных для RAG, чтобы поддерживать такой поиск
Определение минимального набора уточняющих вопросов для каждой категории проблем
Готов развивать любое из этих направлений — без кода, пока вы не попросите.

Совершенно верно — и это ключевой момент в диагностике проблем 3D-печати.

Пользователь часто не знает терминов ("вот эти ниточки", "как будто слои сползают", "деталь вся в пузырях"), но фотография + короткое пояснение дают гораздо больше информации, чем текстовое описание. Поэтому система должна быть готова к мультимодальному входу.

📸 Как интегрировать фото в интерактивное консультирование
1. Поддержка загрузки изображений
Пользователь присылает фото дефекта (например, борозды на поверхности, отслоение углов, «паутина»)
Добавляет краткий комментарий: «Печатаю PLA, Ender-3, такие нити везде»
2. Анализ изображения (локально или гибридно)
Используется мультимодальная модель — например, llava через Ollama
Модель описывает дефект на техническом языке:
«На изображении видна stringing (нитевидное вытягивание пластика между отдельными частями модели). Характерно для: высокой температуры сопла, недостаточного retractions, отсутствия combing.»

→ Это превращает визуальный симптом в структурированный диагностический признак.

3. Диалог продолжается на основе анализа фото
Система теперь уже знает тип проблемы и задаёт точные уточнения:

«По фото похоже на stringing («сопли»).
— У вас включён retraction в слайсере?
— Какая длина и скорость отвода?
— Печатаете в закрытом корпусе или при сквозняке?»

→ Пользователю не нужно объяснять, что такое stringing — система сама распознала и перевела на понятный ему язык.

4. Поиск в базе знаний с учётом визуального контекста
Теперь RAG ищет:

Статьи по stringing + Ender-3 + PLA
Обзоры настройки retraction в Cura/PrusaSlicer
Кейсы: «Как я победил сопли на Anycubic»
→ Результат — конкретные параметры:

«Попробуйте: retraction 6 мм, скорость 45 мм/с, enable combing = "Not in Skin"»

ссылка: Как настроить retraction для PLA
🔁 Гибридный поток: текст + фото
Шаг
Ввод
Система делает
1
Фото + «всё в ниточках»
Распознаёт дефект → предлагает термин («это, скорее всего, stringing»)
2
Пользователь подтверждает или уточняет
Запрашивает параметры принтера/материала
3
Получает контекст
Ищет решения в RAG по (stringing + модель + материал)
4
Даёт пошаговую рекомендацию
С параметрами, чек-листом, ссылкой
💡 Почему это критически важно
Более 70% жалоб на 3D-печать — визуальные (дефекты поверхности, геометрии, структуры)
Пользователи не инженеры — они не знают терминов, но видят проблему
Фото + ИИ → мост между наблюдением и технической диагностикой
🧩 Техническая реализация (без кода)
Интерфейс поддерживает загрузку изображений (Telegram, веб-чат)
Модель: llava (локально, через Ollama) — для первичного анализа
Фолбэк: если llava не уверен — запрашивает у пользователя уточнение:
«На фото похоже на warping или отслоение первого слоя?»

Результат анализа фото становится частью контекста для RAG
Gemini 2.5/3 может использоваться для более глубокого анализа (если картинка сложная или нечёткая)
Такой подход превращает ассистента из «справочной системы» в диагностического партнёра, который:

Видит то же, что и пользователь
Говорит на его языке
Ведёт к решению шаг за шагом
Если это соответствует вашему видению — можем перейти к проектированию:

типов визуальных дефектов для распознавания,
структуры диалоговых сценариев с фото,
или интеграции llava в RAG-контекст

---

## 6. Техническая архитектура и переиспользование кода

### 6.1 Переиспользование кода из существующих проектов

#### ✅ sql4A — FastAPI сервисы для KB (~70% переиспользования)
**Источник:** `../sql4A/`

**Что использовать:**
- ✅ **FastAPI структура** (`src/api/main.py`) — ~90% переиспользования
  - Endpoints для KB (`/training/*`)
  - Модели запросов/ответов (Pydantic)
  - Обработка ошибок, логирование, CORS
- ✅ **RAG pipeline** (`src/services/query_service.py`) — ~70% переиспользования
  - Семантический поиск в векторной БД
  - Гибридный поиск (векторный + фильтры по метаданным)
  - Формирование контекста для LLM
- ✅ **Работа с векторной БД** (`src/vanna/vanna_pgvector_native.py`) — ~60% переиспользования
  - Методы добавления данных (DDL, Documentation, Q/A)
  - Структура метаданных в JSON
  - ⚠️ Адаптировать под Qdrant (или использовать pgvector как есть)

**Адаптация:**
- Заменить SQL генерацию на диагностику 3D-печати
- Адаптировать метаданные под домен (`problem_type`, `printer_models`, `materials`)
- Адаптировать endpoints (`/query` → `/diagnose`)

**Подробности:** см. `SQL4A_REUSE_ANALYSIS.md`

#### ✅ steccom-rag-lk — Gemini через ProxyAPI.ru для работы с картинками
**Источник:** `../../steccom-rag-lk/`

**Что использовать:**
- ✅ **Настройка ProxyAPI** (`setup_proxyapi.py`)
  - Конфигурация через `config.env`
  - Поддержка Gemini через ProxyAPI.ru
- ✅ **Интеграция Gemini для анализа изображений**
  - Использование `gemini-3-pro-preview` через ProxyAPI
  - Мультимодальные запросы (текст + изображение)

**Применение для 3dtoday:**
- Использовать Gemini 3 через ProxyAPI.ru как fallback для сложных изображений
- Локально: llava через Ollama (первичный анализ)
- Fallback: Gemini 3 через ProxyAPI.ru (если llava не уверен)

**Конфигурация:**
```env
# config.env
PROXYAPI_BASE_URL=https://api.proxyapi.ru/google
PROXYAPI_API_KEY=your_api_key
GEMINI_MODEL=gemini-3-pro-preview
```

#### ✅ sam2seg — Мультимодальная RAG для изображений
**Источник:** `../sam2seg/`

**Что использовать:**
- ✅ **MultimodalRAG** (`src/rag/multimodal_rag.py`) — готовый класс для мультимодальной RAG
  - Индексация изображений с текстовыми описаниями
  - Поиск похожих примеров по изображению + текст
  - Поддержка OpenCLIP для эмбеддингов изображений
  - Комбинированные эмбеддинги (image + annotation + text)
- ✅ **Gemini клиент** (`src/llm/gemini_client.py`)
  - Работа с Gemini через ProxyAPI.ru
  - Мультимодальные запросы (vision)
  - Поддержка `gemini-3-pro-preview`
- ✅ **LLM Manager** (`src/llm/llm_manager.py`)
  - Универсальный менеджер для разных провайдеров
  - Fallback цепочка (Ollama → ProxyAPI → OpenAI)
  - Автоматический выбор лучшей модели для задачи

**Применение для 3dtoday:**
- Использовать `MultimodalRAG` для индексации изображений дефектов
- Комбинировать эмбеддинги изображений дефектов с текстовыми описаниями
- Поиск похожих случаев по фото + текст пользователя

**Пример использования:**
```python
from src.rag.multimodal_rag import get_multimodal_rag

rag = get_multimodal_rag()
await rag.initialize()

# Индексация примера дефекта
await rag.index_example(
    image_path="stringing_pla_ender3.jpg",
    annotation_path=None,
    prompt="stringing defect on PLA print",
    pathology_name="stringing",
    metadata={
        "printer_model": "Ender-3",
        "material": "PLA",
        "problem_type": "stringing"
    }
)

# Поиск похожих случаев
similar = await rag.search_similar(
    image_path="user_photo.jpg",
    pathology_name="stringing",
    limit=5
)
```

### 6.2 Архитектура мультимодальной RAG

**Подход из sam2seg:**
1. **Эмбеддинги изображений** — OpenCLIP (ViT-B-16) или multimodal-embeddings
2. **Эмбеддинги текста** — Sentence Transformers (rubert-base или multilingual-e5)
3. **Комбинированные эмбеддинги** — объединение image + text для лучшего поиска
4. **Векторное хранилище** — Qdrant (рекомендуется) или pgvector

**Структура данных в KB:**
```json
{
  "id": "stringing_pla_001",
  "image_path": "stringing_pla_ender3.jpg",
  "problem_type": "stringing",
  "printer_models": ["Ender-3"],
  "materials": ["PLA"],
  "symptoms": ["ниточки", "сопли"],
  "search_text": "stringing defect PLA Ender-3 retraction",
  "image_embedding": [...],
  "text_embedding": [...],
  "combined_embedding": [...],
  "metadata": {
    "solution": "retraction 6mm, speed 45mm/s",
    "source_url": "https://3dtoday.ru/..."
  }
}
```

### 6.3 Идеология стартового проекта ⚠️ **РЕАЛИСТИЧНЫЙ ПЛАН**

**Принципы проекта:**
1. **Срок: 2 недели** — реалистичное планирование, без фантазий
2. **Только факты из KB** — не выдумываем, используем проверенную информацию
3. **Регулярное обновление KB** — актуализация данных
4. **Удобный интерфейс с фото** — приоритет на UX
5. **Меньше, но качественнее** — качество важнее количества
6. **Минимальное количество документов** — только необходимое
7. **Актуализация и удаление мусора** — поддержка чистоты KB
8. **Развитие и поддержка KB** — долгосрочная перспектива

#### MVP на 2 недели (реалистичный план)

**Неделя 1: Базовая функциональность**
- День 1-2: **Ручной отбор 5-10 статей** (НЕ автоматический парсинг)
  - Топ-3 проблемы: stringing, warping, layer separation
  - Только статьи с конкретными решениями
  - Ручное извлечение метаданных (не через LLM)
- День 3: FastAPI backend (адаптация из sql4A)
- День 4: Vision Agent (llava через Ollama)
- День 5: Streamlit интерфейс (MVP)

**Неделя 2: Улучшение и тестирование**
- День 6-7: Улучшение диагностики и RAG
- День 8: Ручное тестирование (5-10 запросов)
- День 9: Поддержка KB (добавление/удаление статей)
- День 10: Финальная полировка и документация

**Результат MVP:**
- ✅ 5-10 качественных статей в KB
- ✅ Рабочая диагностика на основе KB
- ✅ Анализ изображений (llava)
- ✅ Удобный интерфейс (Streamlit)
- ✅ Механизм поддержки KB

**Что НЕ будет в MVP:**
- ❌ Автоматический парсинг (только ручной отбор)
- ❌ Сложная интерактивная диагностика (базовый диалог)
- ❌ Много статей (только 5-10 качественных)
- ❌ Автоматическое тестирование (только ручное)

**Подробности:** см. `REALISTIC_MVP_PLAN.md` ⭐

### 6.4 Технологический стек (финальный)

**Backend:**
- **FastAPI** — переиспользовать из sql4A (~90%)
- **Qdrant** — векторная БД (или pgvector из sql4A)
- **MultimodalRAG** — переиспользовать из sam2seg

**Vision/Analysis:**
- **llava (Ollama)** — локальный первичный анализ изображений
- **Gemini 3 через ProxyAPI.ru** — fallback для сложных случаев (из steccom-rag-lk)
- **OpenCLIP** — эмбеддинги изображений (из sam2seg)

**LLM для диалога:**
- **Llama 3.1 70B (Ollama)** — основной для диагностики
- **Gemini 3 через ProxyAPI.ru** — fallback (из steccom-rag-lk)

**RAG:**
- **MultimodalRAG** — из sam2seg (готовый класс)
- **Гибридный поиск** — векторный + фильтры по метаданным (из sql4A)

**Frontend:**
- **Streamlit** — MVP (быстрый старт)
- **React** — production (позже)

### 6.5 План реализации (РЕАЛИСТИЧНЫЙ на 2 недели)

**⚠️ Важно:** План пересмотрен с учетом реальных ограничений. См. `REALISTIC_MVP_PLAN.md`

**Неделя 1: Базовая функциональность (5 дней)**
- День 1-2: Ручной отбор 5-10 статей + индексация в Qdrant
- День 3: FastAPI backend (адаптация из sql4A)
- День 4: Vision Agent (llava через Ollama)
- День 5: Streamlit интерфейс (MVP)

**Неделя 2: Улучшение и тестирование (5 дней)**
- День 6-7: Улучшение диагностики и RAG
- День 8: Ручное тестирование
- День 9: Поддержка KB (добавление/удаление)
- День 10: Финальная полировка

**Принципы:**
- ✅ Ручной отбор статей (НЕ автоматический парсинг)
- ✅ Только факты из KB (не выдумываем)
- ✅ Меньше, но качественнее (5-10 статей)
- ✅ Регулярное обновление KB (актуализация)

---

## 7. Конфигурация проекта

### 7.1 Использование config.env из sql4A

**Источник:** `../sql4A/config.env`

**Принцип:** Проект использует те же ключи и настройки, что и sql4A, для единообразия и упрощения управления.

**Файлы конфигурации:**
- `config.env` — рабочая конфигурация (использует ключи из sql4A)
- `config.env.example` — пример конфигурации для новых установок

**Ключевые настройки из sql4A:**
```env
# Используется тот же ProxyAPI ключ
OPENAI_API_KEY=your_proxyapi_key_here
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Используется та же БД (если используется pgvector)
DATABASE_URL=postgresql://postgres:1234@localhost:5432/test_docstructure

# Используется та же модель эмбеддингов
HF_MODEL_NAME=intfloat/multilingual-e5-base
```

**Дополнительные настройки для 3dtoday:**
```env
# Gemini через ProxyAPI.ru (для анализа изображений)
GEMINI_API_KEY=your_proxyapi_key_here
GEMINI_BASE_URL=https://api.proxyapi.ru/google
GEMINI_MODEL=gemini-3-pro-preview

# Qdrant (векторная БД)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=kb_3dtoday

# Ollama (локальные модели)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:70b
OLLAMA_VISION_MODEL=llava
```

**Загрузка конфигурации в коде:**
```python
# Используется тот же подход, что и в sql4A
from dotenv import load_dotenv
from pathlib import Path

# Загружаем config.env из текущего проекта
load_dotenv(dotenv_path=Path(__file__).resolve().parents[0] / "config.env")

# Или можно загрузить из sql4A (если нужны только ключи)
# load_dotenv(dotenv_path=Path(__file__).resolve().parents[1] / "sql4A" / "config.env")
```

### 7.2 Структура конфигурации

**Секции конфигурации:**
1. **LLM Provider** — выбор провайдера (Ollama/ProxyAPI/Gemini)
2. **OpenAI/ProxyAPI** — настройки для ProxyAPI.ru (из sql4A)
3. **Gemini** — настройки для Gemini через ProxyAPI.ru
4. **Ollama** — настройки локальных моделей
5. **Vector DB** — настройки Qdrant или pgvector
6. **Embeddings** — настройки моделей эмбеддингов
7. **RAG** — настройки RAG pipeline
8. **Agents** — настройки агентов (Diagnostic, Vision)

**Приоритет загрузки:**
1. Переменные окружения (высший приоритет)
2. `config.env` в корне проекта 3dtoday
3. `config.env` из sql4A (fallback для ключей)

---

## 8. Парсинг, тестирование и архитектура агентов

### 8.1 Интеллектуальный парсинг сайта 3dtoday.ru

**Цель:** Извлечь только релевантную информацию, минимизировав загрузку "мусора" в KB.

**Многоуровневая фильтрация:**

1. **Структурная фильтрация** (до парсинга)
   - Фильтрация по разделам сайта
   - Исключение нерелевантных разделов (Акции, Песочница, Объявления)
   - Приоритизация разделов (Техничка → высокий приоритет)

2. **Контентная фильтрация** (после парсинга)
   - Использование LLM для анализа релевантности
   - Оценка релевантности (relevance_score > 0.7)
   - Проверка наличия конкретных решений

3. **Извлечение метаданных**
   - Структурированные метаданные (problem_type, printer_models, materials)
   - Автоматическое извлечение через LLM
   - Валидация метаданных

**Подробности:** см. `PARSING_AND_TESTING_STRATEGY.md` (раздел 1)

### 8.2 Тестирование KB на релевантность

**Методология (из ai_report и steccom-rag-lk):**

**Метрики качества:**
- **Top-k Hit Rate:** Доля запросов, где релевантная статья в top-k
  - Top-1: > 0.6 (60%)
  - Top-3: > 0.8 (80%)
  - Top-5: > 0.9 (90%)
- **Relevance Score:** Процент найденных ключевых слов (> 0.7)
- **MRR (Mean Reciprocal Rank):** > 0.7
- **Diagnostic Accuracy:** Точность диагностики (> 0.8)

**Процесс тестирования:**
1. Генерация тестовых вопросов на основе статей (LLM)
2. Поиск в KB для каждого вопроса
3. Оценка релевантности результатов
4. Расчет метрик качества
5. Итеративное улучшение на основе результатов

**Подробности:** см. `PARSING_AND_TESTING_STRATEGY.md` (раздел 2)

### 8.3 Архитектура взаимодействия агентов

**Принципы согласованности:**

1. **Единый контекст сессии (SessionContext)**
   - Все агенты работают с одним объектом контекста
   - История диалога сохраняется в контексте
   - Метаданные передаются между агентами

2. **Четкие роли агентов**
   - Vision Agent: анализ изображений → добавляет в контекст
   - Diagnostic Agent: диагностика → использует контекст, обновляет состояние
   - Retrieval Agent: поиск в KB → использует фильтры из контекста

3. **Согласованные метаданные**
   - Единый формат метаданных для всех агентов
   - Валидация данных между агентами
   - Разрешение конфликтов (если разные агенты дают разные результаты)

**Архитектура:**
```
Orchestrator Agent
    ├── Vision Agent (анализ изображений)
    │   └── Добавляет vision_result в SessionContext
    ├── Diagnostic Agent (диагностика)
    │   └── Использует SessionContext, обновляет diagnostic_state
    ├── Retrieval Agent (поиск в KB)
    │   └── Использует filters из SessionContext
    └── KB Curator Agent (цензурирование и подготовка KB) ⭐
        └── Работает отдельно от пользовательских запросов
            - Валидация извлеченной информации
            - Извлечение метаданных
            - Генерация Q/A пар
            - Фильтрация мусора
```

**KB Curator Agent:**
- **Роль:** Цензурирование информации от парсера и подготовка Q/A для KB
- **Использование:** При добавлении новых статей в KB (не в реальном времени)
- **Функции:**
  - Валидация релевантности и качества статей
  - Автоматическое извлечение метаданных через LLM
  - Генерация Q/A пар из статей (3-5 пар на статью)
  - Фильтрация мусора (отклоняет нерелевантные статьи)

**Подробности:** 
- Взаимодействие агентов: см. `PARSING_AND_TESTING_STRATEGY.md` (раздел 3)
- KB Curator Agent: см. `KB_CURATOR_AGENT.md` ⭐

---

## 9. Следующие шаги (РЕАЛИСТИЧНЫЙ ПЛАН на 2 недели)

### День 1-2: KB и инфраструктура
1. ✅ Настроить проект (скопировать FastAPI из sql4A)
2. ✅ **Ручной отбор 5-10 статей** из 3dtoday.ru
   - Топ-3 проблемы: stringing, warping, layer separation
   - Только статьи с конкретными решениями
3. ✅ **KB Curator Agent** (цензурирование и подготовка Q/A)
   - Валидация извлеченной информации
   - Автоматическое извлечение метаданных через LLM
   - Генерация Q/A пар из статей (3-5 пар на статью)
   - Фильтрация мусора (только качественные статьи)
4. ✅ Индексация в Qdrant (или pgvector из sql4A)
   - Статьи + Q/A пары

### День 3: Backend
5. ✅ Адаптация FastAPI из sql4A
6. ✅ Базовый Diagnostic Service (RAG pipeline)
7. ✅ Тестирование API

### День 4: Vision
8. ✅ Интеграция llava через Ollama
9. ✅ Fallback на Gemini (если нужно)

### День 5: Frontend
10. ✅ Streamlit интерфейс (базовый чат + загрузка фото)

### День 6-7: Улучшение
11. ✅ Улучшение RAG поиска (фильтрация по метаданным)
12. ✅ Улучшение ответов (синтез через LLM)

### День 8: Тестирование
13. ✅ Ручное тестирование (5-10 запросов)
14. ✅ Оценка релевантности ответов

### День 9: Поддержка KB
15. ✅ Простой интерфейс для управления KB
16. ✅ Механизм добавления/удаления статей

### День 10: Финальная полировка
17. ✅ Улучшение UX
18. ✅ Документация (README, KB_MAINTENANCE.md)

**⚠️ Что НЕ делаем в MVP:**
- ❌ Автоматический парсинг (только ручной отбор)
- ❌ Сложная интерактивная диагностика (базовый диалог)
- ❌ Много статей (только 5-10 качественных)
- ❌ Автоматическое тестирование (только ручное)
- ❌ Сложная архитектура агентов (упрощенная версия)

**Подробности:** см. `REALISTIC_MVP_PLAN.md` ⭐

**Документация:**
- `REALISTIC_MVP_PLAN.md` — **РЕАЛИСТИЧНЫЙ ПЛАН на 2 недели** ⭐⭐⭐
- `MCP_AGENT_APPROACH.md` — **Подход MCP Agent (красивый паттерн)** ⭐⭐⭐
- `KB_CURATOR_AGENT.md` — **Агент-куратор KB (цензурирование и Q/A)** ⭐⭐
- `KB_MAINTENANCE.md` — **Руководство по поддержке и развитию KB** ⭐⭐
- `RECOMMENDATIONS.md` — детальные рекомендации (для будущего развития)
- `KB_ARCHITECTURE_COMPARISON.md` — сравнение с ai_report
- `SQL4A_REUSE_ANALYSIS.md` — анализ переиспользования sql4A
- `PARSING_AND_TESTING_STRATEGY.md` — стратегия парсинга (для будущего развития)
- `config.env` — рабочая конфигурация (использует ключи из sql4A)
- `config.env.example` — пример конфигурации
- `README_CONFIG.md` — документация по конфигурации

**⚠️ Важно:** 
- `REALISTIC_MVP_PLAN.md` — основной план для реализации (2 недели)
- `MCP_AGENT_APPROACH.md` — **красивый подход с MCP** (рекомендуется для архитектуры агентов) ⭐
- `KB_MAINTENANCE.md` — руководство по поддержке KB (регулярное обновление, удаление мусора)
- Остальные документы — для будущего развития, использовать только необходимое

**💡 Рекомендация по архитектуре:**
- Использовать подход MCP Agent из `../mcp/` проекта
- Инструменты KB определяются декларативно через `@mcp.tool()`
- LangGraph автоматически управляет использованием инструментов
- Чистая архитектура с разделением сервера и клиента
