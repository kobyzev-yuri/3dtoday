# Руководство по использованию разных провайдеров парсинга

## 🎯 Выбор провайдера

### 1. Ollama (упрощенный) - для ограниченных ресурсов

**Когда использовать:**
- Ограниченные ресурсы системы
- Быстрая обработка без глубокого анализа
- Статьи без изображений или с простым текстом

**Как использовать:**
```bash
# Через API
curl -X POST "http://localhost:8000/api/kb/articles/parse" \
  -H "Content-Type: application/json" \
  -d '{
    "source": "https://example.com/article",
    "source_type": "url",
    "llm_provider": "ollama"
  }'

# Через тест
python tools/test_all_providers.py --ollama --url "https://example.com/article"
```

**Особенности:**
- ✅ Быстрый парсинг
- ✅ Меньше нагрузка на систему
- ⚠️ Нет глубокого анализа изображений
- ⚠️ Упрощенная проверка релевантности

---

### 2. Gemini (РЕКОМЕНДУЕТСЯ для статей с изображениями) ⭐

**Когда использовать:**
- Статьи с изображениями дефектов
- Нужен анализ изображений через Vision API
- Требуется создание абстрактов изображений для KB

**Как использовать:**
```bash
# Через API (с анализом изображений)
curl -X POST "http://localhost:8000/api/kb/articles/parse_with_llm" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/",
    "llm_provider": "gemini",
    "model": "gemini-3-pro-preview"
  }'

# Через тест
python tools/test_all_providers.py --gemini --url "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/"
```

**Особенности:**
- ✅ Анализ изображений через Gemini Vision API
- ✅ Создание абстрактов изображений
- ✅ Добавление контекста изображений в KB
- ✅ Высокое качество анализа релевантности
- ✅ Фильтрация нерелевантных изображений

**Что проверяется:**
- Изображения анализируются через Gemini Vision API
- Проверяется релевантность к 3D-печати
- Создаются абстракты найденного в изображениях
- Абстракты добавляются в KB как контекст

---

### 3. OpenAI (альтернативный вариант)

**Когда использовать:**
- Нет доступа к Gemini
- Нужен высококачественный анализ текста
- Альтернатива Gemini для парсинга

**Как использовать:**
```bash
# Через API
curl -X POST "http://localhost:8000/api/kb/articles/parse_with_llm" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/article",
    "llm_provider": "openai",
    "model": "gpt-4o"
  }'

# Через тест
python tools/test_all_providers.py --openai --url "https://example.com/article"
```

**Особенности:**
- ✅ Высокое качество анализа
- ✅ Хорошая проверка релевантности
- ⚠️ Нет анализа изображений (используется Gemini для Vision)

---

## 📷 Анализ изображений и абстракты

### Как работает анализ изображений

1. **Извлечение изображений** из статьи
2. **Анализ через Gemini Vision API** (для каждого изображения)
3. **Проверка релевантности** к 3D-печати
4. **Создание абстракта** найденного в изображении
5. **Добавление в KB** как контекст

### Что добавляется в KB

**Для каждого релевантного изображения:**
- Абстракт (описание найденного)
- Тип проблемы (если определен)
- Модели принтеров (если видны)
- Материалы (если видны)
- Релевантность (score)

**Важно:**
- ✅ Только релевантные изображения добавляются
- ✅ Не замусориваем KB несущественной информацией
- ✅ Абстракты помогают в мультимодальном поиске

---

## 🚫 Фильтрация нерелевантного контента

### Критерии отклонения

**Автоматически отклоняются:**
- `relevance_score < 0.6`
- `is_relevant == False`
- Статьи не по теме (музыка, личные предпочтения, оффтоп)
- Изображения не связанные с 3D-печатью

### Тест фильтрации

```bash
# Проверить, что нерелевантные статьи отклоняются
python tools/test_all_providers.py --filtering
```

**Примеры нерелевантного контента:**
- "Какую музыку Вы слушаете при моделировании?"
- "Какой ваш любимый цвет?"
- Обсуждения не связанные с 3D-печатью

---

## 🧪 Полное тестирование

### Тест всех провайдеров

```bash
# Все варианты
python tools/test_all_providers.py --all

# С добавлением в KB
python tools/test_all_providers.py --all --add

# Только Gemini (самый важный для изображений)
python tools/test_all_providers.py --gemini --url "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/"
```

### Что проверяется

1. **Парсинг работает** - статья распарсена
2. **Релевантность проверена** - relevance_score рассчитан
3. **Изображения проанализированы** (для Gemini) - абстракты созданы
4. **Фильтрация работает** - нерелевантные отклоняются
5. **KB не замусорена** - только релевантный контент

---

## 💡 Рекомендации

### Для статей с изображениями:
- ✅ **Используйте Gemini** - лучший анализ изображений
- ✅ Проверяйте абстракты изображений в review
- ✅ Убедитесь, что изображения релевантны

### Для простых статей без изображений:
- ✅ **Используйте Ollama** - быстрее и проще
- ✅ Подходит для ограниченных ресурсов

### Для высококачественного анализа:
- ✅ **Используйте Gemini или OpenAI** - лучшая проверка релевантности

---

## 📊 Сравнение провайдеров

| Провайдер | Скорость | Качество анализа | Анализ изображений | Ресурсы |
|-----------|----------|------------------|---------------------|---------|
| Ollama    | ⚡⚡⚡ Быстро | ⭐⭐ Среднее | ❌ Нет | 💚 Низкие |
| Gemini    | ⚡⚡ Средне | ⭐⭐⭐ Высокое | ✅✅✅ Да (Vision API) | 💛 Средние |
| OpenAI    | ⚡⚡ Средне | ⭐⭐⭐ Высокое | ❌ Нет | 💛 Средние |

---

## 🔧 Настройка

### Конфигурация в `config.env`

```bash
# LLM Provider (по умолчанию)
LLM_PROVIDER=gemini  # или ollama, openai

# Gemini (для анализа изображений)
GEMINI_API_KEY=your_key
GEMINI_BASE_URL=https://api.proxyapi.ru/google
GEMINI_MODEL=gemini-3-pro-preview

# Ollama (упрощенный)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:70b
OLLAMA_VISION_MODEL=llava

# OpenAI (альтернатива)
OPENAI_API_KEY=your_key
OPENAI_MODEL=gpt-4o
```

---

## ✅ Чек-лист использования

- [ ] Выбран правильный провайдер для задачи
- [ ] Для изображений используется Gemini
- [ ] Проверена релевантность (relevance_score >= 0.6)
- [ ] Абстракты изображений созданы (для Gemini)
- [ ] Нерелевантный контент отклонен
- [ ] KB не замусорена несущественной информацией


