# Руководство по тестированию URL с изображениями

## 🚀 Быстрый старт

### 1. Проверка доступности API

Перед запуском тестов убедитесь, что FastAPI сервер запущен:

```bash
# Проверка
curl http://localhost:8000/health

# Если сервер не запущен, запустите его:
cd /mnt/ai/cnn/3dtoday
PYTHONPATH=. uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 📋 Способы тестирования

### Способ 1: Через скрипт (рекомендуется)

#### Тест одного URL:

```bash
cd /mnt/ai/cnn/3dtoday
./tools/test_url_images.sh "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/" gemini
```

#### Тест нескольких URL из `image_urls.json`:

```bash
cd /mnt/ai/cnn/3dtoday
./tools/test_multiple_urls.sh
```

**Что делает скрипт:**
- ✅ Проверяет доступность API
- ✅ Извлекает URL из `knowledge_base/image_urls.json`
- ✅ Запускает тесты для каждого URL
- ✅ Показывает результаты

---

### Способ 2: Прямой запуск Python скрипта

```bash
cd /mnt/ai/cnn/3dtoday
python3 tools/test_url_with_images.py "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/" --provider gemini
```

**Параметры:**
- `URL` - URL для тестирования (обязательный)
- `--provider` - Провайдер LLM: `gemini` (по умолчанию), `openai`, `ollama`

---

### Способ 3: Через curl (для быстрой проверки)

```bash
# Парсинг URL через LLM
curl -X POST "http://localhost:8000/api/kb/articles/parse_with_llm" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/",
    "llm_provider": "gemini",
    "model": "gemini-2.0-flash-exp"
  }'
```

---

## 📊 Что проверяется

### При тестировании URL:

1. **Парсинг URL**
   - Извлечение заголовка
   - Извлечение контента
   - Извлечение изображений

2. **Анализ релевантности**
   - `relevance_score` (должен быть >= 0.6)
   - `quality_score`
   - `is_relevant` (должен быть `true`)
   - `has_valuable_info`
   - `decision` (approve/reject/needs_review)

3. **Обработка изображений**
   - Скачивание изображений из URL
   - Анализ через Gemini Vision API
   - Проверка релевантности к 3D-печати
   - Создание абстрактов
   - Индексация в KB

4. **Добавление в KB**
   - Создание article_id
   - Индексация текста
   - Индексация изображений
   - Сохранение метаданных

---

## 🎯 Примеры тестовых URL

### Приоритетные источники (из `image_urls.json`):

1. **Simplify3D Stringing** (лучший пример):
   ```
   https://www.simplify3d.com/resources/print-quality-troubleshooting/stringing-or-oozing/
   ```

2. **All3DP Warping**:
   ```
   https://all3dp.com/2/3d-printing-warping-how-to-fix-it/
   ```

3. **Prusa Knowledge Base**:
   ```
   https://help.prusa3d.com/article/layer-separation-and-splitting_1258
   ```

---

## ⚙️ Настройка

### Переменные окружения (в `config.env`):

```bash
# Gemini API (для анализа изображений)
GEMINI_API_KEY=your_api_key
GEMINI_BASE_URL=https://api.proxyapi.ru/google
GEMINI_MODEL=gemini-2.0-flash-exp

# OpenAI (альтернатива)
OPENAI_API_KEY=your_api_key
OPENAI_MODEL=gpt-4o

# Ollama (для ограниченных ресурсов)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
```

---

## 🔍 Отладка

### Проверка логов:

```bash
# Логи FastAPI сервера (в консоли где запущен сервер)
# Или проверьте файлы логов в backend/logs/
```

### Проверка результатов:

```bash
# Проверка статистики KB
python3 tools/check_kb_stats.py

# Просмотр статьи по ID
python3 tools/view_article.py <article_id>
```

---

## ❌ Типичные проблемы

### 1. API сервер недоступен

**Ошибка:** `Connection refused` или `HTTP 500`

**Решение:**
```bash
cd /mnt/ai/cnn/3dtoday
PYTHONPATH=. uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

### 2. Gemini API не настроен

**Ошибка:** `Vision API недоступен`

**Решение:**
- Проверьте `GEMINI_API_KEY` в `config.env`
- Убедитесь, что ключ валидный

---

### 3. Изображения не анализируются

**Проблема:** Изображения не индексируются

**Решение:**
- Проверьте доступность Gemini Vision API
- Проверьте логи на ошибки скачивания изображений
- Убедитесь, что URL изображений доступны

---

## 📝 Примечания

- Тесты могут занимать время (особенно анализ изображений)
- Рекомендуется тестировать по одному URL за раз для отладки
- Скрипт `test_multiple_urls.sh` автоматически делает паузы между тестами
- Изображения сохраняются во временную директорию `/tmp/kb_images/`

---

## 🔗 Связанные документы

- `tools/URL_IMAGES_FIXES.md` - Детали исправлений
- `tools/FIXES_VERIFICATION.md` - Проверка исправлений логики
- `knowledge_base/image_urls.json` - Список URL для тестирования
- `docs/KB_MANAGEMENT.md` - Управление KB


