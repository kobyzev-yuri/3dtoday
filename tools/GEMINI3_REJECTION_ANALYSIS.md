# Анализ причины отклонения документа Gemini3

**Дата:** 2025-12-09  
**Проблема:** Документ получил `relevance_score=0.95`, `quality_score=0.90`, `decision=approve`, но `is_relevant=False`

---

## 🔍 Анализ логики принятия решений

### Данные из теста PDF:
- `relevance_score`: **0.95** ✅ (высокая)
- `quality_score`: **0.90** ✅ (высокая)
- `is_relevant`: **False** ❌ (не релевантен?)
- `has_valuable_info`: **True** ✅
- `is_duplicate`: **False** ✅
- `decision`: **approve** ✅

### Логика `_make_decision` (строки 470-526):

#### 1️⃣ Проверка дубликата (строка 486):
```python
if is_duplicate:
    return {"decision": "reject", ...}
```
✅ **Результат:** Не дубликат, продолжаем

#### 2️⃣ Проверка релевантности (строка 496):
```python
if not is_relevant or relevance_score < 0.6:
    return {"decision": "reject", ...}
```
❌ **ПРОБЛЕМА:** Эта проверка **ДОЛЖНА** была вернуть `REJECT`!
- `not is_relevant` = `not False` = `True`
- Условие `True OR False` = `True`
- Должно вернуться `REJECT`

**Но в тесте вернулось `APPROVE`!** Это означает баг в логике.

#### 3️⃣ Проверка ценной информации (строка 503):
```python
if not has_valuable_info or quality_score < 0.6:
    return {"decision": "reject", ...}
```
✅ **Результат:** Есть ценная информация, продолжаем

#### 4️⃣ Проверка одобрения (строка 510):
```python
if relevance_score >= 0.7 and quality_score >= 0.7 and not is_duplicate:
    return {"decision": "approve", ...}
```
⚠️ **ПРОБЛЕМА:** Эта проверка **НЕ учитывает `is_relevant`**!
- Проверяет только `relevance_score >= 0.7` ✅
- Проверяет только `quality_score >= 0.7` ✅
- Проверяет только `not is_duplicate` ✅
- **НЕ проверяет `is_relevant`** ❌

---

## 🐛 Обнаруженные проблемы

### Проблема 1: Логика не учитывает `is_relevant` при одобрении

**Строка 510:**
```python
if relevance_score >= 0.7 and quality_score >= 0.7 and not is_duplicate:
    return {"decision": "approve", ...}
```

**Должно быть:**
```python
if relevance_score >= 0.7 and quality_score >= 0.7 and is_relevant and not is_duplicate:
    return {"decision": "approve", ...}
```

### Проблема 2: Противоречие между проверками

- **Строка 496:** Проверяет `is_relevant` и должна вернуть `REJECT` при `is_relevant=False`
- **Строка 510:** Не проверяет `is_relevant` и может вернуть `APPROVE` даже при `is_relevant=False`

**Результат:** Если `is_relevant=False`, но `relevance_score >= 0.7`, то:
- Проверка на строке 496 должна вернуть `REJECT`
- Но почему-то код дошел до строки 510 и вернул `APPROVE`

---

## 💡 Возможные причины

### Причина 1: `is_relevant` не возвращается в результате

В методе `review_and_decide` (строка 190-201) возвращается:
```python
return {
    "decision": decision["decision"],
    "reason": decision["reason"],
    "relevance_score": relevance_check.get("score", 0.0),
    "quality_score": relevance_check.get("quality_score", 0.0),
    ...
}
```

**Проблема:** `is_relevant` **НЕ возвращается** в результате! Он есть только в `relevance_check`, но не попадает в финальный результат.

### Причина 2: Gemini3 вернула противоречивые данные

Gemini3 в методе `_check_relevance` вернула:
- `score: 0.95` (высокая релевантность)
- `is_relevant: False` (не релевантен?)

Это противоречие может быть из-за:
1. Неправильного парсинга JSON ответа Gemini3
2. Неправильного промпта для Gemini3
3. Ошибки в логике определения `is_relevant` в Gemini3

---

## 🔧 Решения

### Решение 1: Исправить логику `_make_decision`

**Файл:** `backend/app/agents/kb_librarian.py`

**Строка 510:**
```python
# БЫЛО:
if relevance_score >= 0.7 and quality_score >= 0.7 and not is_duplicate:

# ДОЛЖНО БЫТЬ:
if relevance_score >= 0.7 and quality_score >= 0.7 and is_relevant and not is_duplicate:
```

### Решение 2: Вернуть `is_relevant` в результате

**Файл:** `backend/app/agents/kb_librarian.py`

**Строка 190-201:**
```python
return {
    "decision": decision["decision"],
    "reason": decision["reason"],
    "relevance_score": relevance_check.get("score", 0.0),
    "quality_score": relevance_check.get("quality_score", 0.0),
    "is_relevant": relevance_check.get("is_relevant", False),  # ДОБАВИТЬ
    "has_valuable_info": relevance_check.get("has_valuable_info", False),  # ДОБАВИТЬ
    ...
}
```

### Решение 3: Улучшить промпт для Gemini3

**Файл:** `backend/app/agents/kb_librarian.py`

**Строка 223-281:** Улучшить промпт, чтобы Gemini3 правильно определяла `is_relevant`:
- Явно указать, что `is_relevant` должен быть `True` при `score >= 0.7`
- Добавить примеры правильного определения `is_relevant`

---

## 📊 Выводы

1. **Основная проблема:** Логика `_make_decision` не учитывает `is_relevant` при одобрении (строка 510)
2. **Вторичная проблема:** `is_relevant` не возвращается в результате `review_and_decide`
3. **Возможная причина:** Gemini3 вернула противоречивые данные (`score=0.95`, но `is_relevant=False`)

**Рекомендация:** Исправить логику `_make_decision`, чтобы она проверяла `is_relevant` при одобрении, и вернуть `is_relevant` в результате `review_and_decide`.


