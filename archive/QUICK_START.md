# Быстрый старт: Запуск интерфейсов

## 🚀 Запуск системы

### Шаг 1: Запуск сервисов

**1. Qdrant (векторная БД):**
```bash
./scripts/start_qdrant.sh
# или
docker compose up -d qdrant
```

**2. Ollama (если используется):**
```bash
ollama serve
```

**3. FastAPI Backend:**
```bash
cd /mnt/ai/cnn/3dtoday
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен: `http://localhost:8000`

### Шаг 2: Запуск интерфейсов

**Административный интерфейс (управление KB):**
```bash
streamlit run frontend/admin_ui.py
```
Откроется: `http://localhost:8501`

**Пользовательский интерфейс (диагностика):**
```bash
streamlit run frontend/user_ui.py --server.port 8502
```
Откроется: `http://localhost:8502`

---

## 📚 Административный интерфейс

### Процесс добавления статьи:

1. Открыть `http://localhost:8501`
2. Заполнить форму:
   - URL статьи (опционально)
   - Заголовок
   - Содержимое
   - Раздел
3. Нажать "Проверить и добавить в KB"
4. Проверить результаты валидации
5. Нажать "Добавить статью в KB"

### Варианты ввода информации:

- **Ручной ввод** - скопировать текст статьи и вставить в форму
- **Автоматическое извлечение метаданных** - система сама извлечет тип проблемы, принтеры, материалы
- **Валидация через LLM** - автоматическая проверка релевантности

---

## 👥 Пользовательский интерфейс

### Процесс диагностики:

1. Открыть `http://localhost:8502`
2. В боковой панели указать (опционально):
   - Модель принтера
   - Материал
3. Описать проблему в текстовом поле
4. Нажать "Получить диагностику"
5. Ответить на уточняющие вопросы (если есть)
6. Получить рекомендации

### Диалог с уточнениями:

Система автоматически определяет, нужны ли уточнения:

- **Если не указана модель принтера** → вопрос: "Какая у вас модель принтера?"
- **Если не указан материал** → вопрос: "Какой материал вы используете?" (с вариантами)
- **Если мало информации** → вопрос: "Опишите проблему подробнее"

Ответы сохраняются в контексте для следующих запросов.

---

## 📝 Примеры использования

### Пример 1: Добавление статьи

```
1. Открыть http://localhost:8501
2. URL: https://3dtoday.ru/...
3. Заголовок: "Как устранить stringing на Ender-3"
4. Содержимое: [текст статьи]
5. Проверить → Добавить
```

### Пример 2: Диагностика

```
1. Открыть http://localhost:8502
2. Принтер: Ender-3
3. Материал: PLA
4. Запрос: "У меня ниточки между деталями"
5. Получить рекомендации
```

---

## 🔧 Проверка работы

**Проверка API:**
```bash
curl http://localhost:8000/health
```

**Проверка статистики KB:**
```bash
curl http://localhost:8000/api/kb/statistics
```

---

## 📚 Дополнительная документация

- `docs/STREAMLIT_INTERFACES_GUIDE.md` - подробное руководство
- `docs/ARTICLE_COLLECTION_PROCESS.md` - процесс сбора статей
- `docs/INTERFACES_ARCHITECTURE.md` - архитектура интерфейсов



