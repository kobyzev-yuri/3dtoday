# Руководство по работе с Streamlit интерфейсами

## 🎯 Обзор

В проекте есть **2 Streamlit интерфейса**, работающих через FastAPI:

1. **Административный интерфейс** (`frontend/admin_ui.py`) - для управления KB
2. **Пользовательский интерфейс** (`frontend/user_ui.py`) - для диагностики проблем

---

## 🚀 Запуск

### 1. Запуск FastAPI Backend

**Обязательно запустить первым!**

```bash
cd /mnt/ai/cnn/3dtoday
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
```

Или через Python:
```bash
python3 backend/app/main.py
```

API будет доступен по адресу: `http://localhost:8000`

### 2. Запуск административного интерфейса

```bash
streamlit run frontend/admin_ui.py
```

Откроется в браузере: `http://localhost:8501`

### 3. Запуск пользовательского интерфейса

```bash
streamlit run frontend/user_ui.py --server.port 8502
```

Откроется в браузере: `http://localhost:8502`

**Примечание:** Используем разные порты, чтобы можно было запустить оба интерфейса одновременно.

---

## 📚 Административный интерфейс

### Назначение
Управление базой знаний: добавление, валидация статей.

### Процесс работы

#### Шаг 1: Ввод данных статьи

1. Откройте `http://localhost:8501`
2. Заполните форму:
   - **URL статьи** (опционально) - ссылка на статью с 3dtoday.ru
   - **Раздел** - выберите из списка (Техничка, 3D-печать, etc.)
   - **Заголовок** (обязательно) - название статьи
   - **Содержимое** (обязательно) - полный текст статьи

#### Шаг 2: Валидация

1. Нажмите "🔍 Проверить и добавить в KB"
2. Система автоматически:
   - Проверит релевантность через LLM
   - Оценит качество статьи
   - Проверит наличие решений
   - Извлечет метаданные (тип проблемы, принтеры, материалы, решения)

#### Шаг 3: Просмотр результатов

Отображаются:
- **Релевантность** (0.0-1.0) - должна быть >= 0.7
- **Качество** (0.0-1.0) - должна быть >= 0.6
- **Есть решения** - должны быть конкретные решения
- **Извлеченные метаданные**:
  - Тип проблемы (stringing, warping, etc.)
  - Модели принтеров
  - Материалы
  - Симптомы
  - Решения с параметрами

#### Шаг 4: Добавление в KB

1. Если статья релевантна - нажмите "💾 Добавить статью в KB"
2. Система проиндексирует статью в Qdrant
3. Статья станет доступна для поиска

### Варианты ввода информации о статьях

#### Вариант 1: Ручной ввод
- Скопировать текст статьи с сайта
- Вставить в форму
- Заполнить метаданные вручную (опционально)

#### Вариант 2: Автоматическое извлечение метаданных
- Система автоматически извлекает метаданные через LLM
- Можно проверить и скорректировать при необходимости

#### Вариант 3: Импорт из JSON
- Подготовить статью в JSON формате
- Использовать API напрямую (не через интерфейс)

---

## 👥 Пользовательский интерфейс

### Назначение
Диагностика проблем 3D-печати для конечных пользователей.

### Процесс работы

#### Шаг 1: Настройка контекста (опционально)

В боковой панели:
- Укажите **модель принтера** (Ender-3, Anycubic Kobra, etc.)
- Выберите **материал** (PLA, PETG, ABS, etc.)

Это улучшит точность диагностики.

#### Шаг 2: Описание проблемы

1. Введите описание проблемы в текстовое поле
2. Примеры:
   - "У меня появляются ниточки между деталями при печати PLA"
   - "Печать отслаивается от стола на Ender-3"
   - "Трещины в слоях при печати PETG"

#### Шаг 3: Получение диагностики

1. Нажмите "🔍 Получить диагностику"
2. Система:
   - Найдет релевантные статьи в KB
   - Сформирует ответ через LLM
   - Определит, нужны ли уточнения

#### Шаг 4: Ответ на уточняющие вопросы

Если система задает вопросы:
- **Модель принтера?** - выберите из списка или введите
- **Материал?** - выберите из списка (PLA, PETG, ABS, etc.)
- **Симптомы?** - опишите подробнее

Ответы сохраняются в контексте для следующих запросов.

#### Шаг 5: Получение рекомендаций

Система предоставит:
- **Конкретные решения** с параметрами
- **Параметры** (температура, скорость, retraction)
- **Ссылки на релевантные статьи** из KB
- **Уверенность** в ответе

### Диалог с пользователем

#### Механизм уточнений

Система автоматически определяет, нужны ли уточнения:

1. **Если не указана модель принтера:**
   - Задается вопрос: "Какая у вас модель принтера?"
   - Ответ сохраняется в контексте

2. **Если не указан материал:**
   - Задается вопрос: "Какой материал вы используете?"
   - Предлагаются варианты: PLA, PETG, ABS, TPU, Другое

3. **Если мало релевантных результатов:**
   - Задается вопрос: "Можете описать проблему подробнее?"
   - Система просит указать симптомы

#### История диалога

- Все сообщения сохраняются в истории
- Контекст сохраняется между запросами
- Можно очистить историю кнопкой "🗑️ Очистить историю"

#### Пример диалога

```
Пользователь: "У меня ниточки при печати"

Система: "Для точной диагностики нужны уточнения:
1. Какая у вас модель принтера?
2. Какой материал вы используете? (PLA, PETG, ABS, etc.)"

Пользователь: [указывает Ender-3, PLA]

Система: "Похоже на stringing (сопли). Рекомендации:
1. Увеличьте retraction до 6 мм
2. Скорость retraction: 45 мм/с
3. Температура сопла: 200°C
..."
```

---

## 🔄 Варианты ввода информации о статьях

### 1. Ручной ввод через интерфейс

**Процесс:**
1. Найти статью на 3dtoday.ru
2. Скопировать текст
3. Вставить в форму административного интерфейса
4. Система автоматически валидирует и извлекает метаданные

**Преимущества:**
- ✅ Простота использования
- ✅ Автоматическая валидация
- ✅ Визуальная обратная связь

### 2. Импорт из JSON

**Формат:**
```json
{
  "title": "Как устранить stringing",
  "content": "Полный текст...",
  "url": "https://3dtoday.ru/...",
  "section": "Техничка",
  "metadata": {
    "problem_type": "stringing",
    "printer_models": ["Ender-3"],
    "materials": ["PLA"]
  }
}
```

**Использование:**
- Через API напрямую: `POST /api/kb/articles/add`
- Можно создать скрипт для массового импорта

### 3. CLI инструмент

**Запуск:**
```bash
python3 backend/app/tools/article_collector.py
```

**Процесс:**
- Интерактивный ввод
- Валидация
- Индексация

---

## 💬 Диалог с пользователем

### Стратегия получения уточнений

#### 1. Определение недостающей информации

Система проверяет:
- ✅ Есть ли модель принтера в контексте?
- ✅ Есть ли материал в контексте?
- ✅ Достаточно ли информации для поиска?
- ✅ Высока ли релевантность найденных статей?

#### 2. Типы уточняющих вопросов

**Вопросы о контексте:**
- Модель принтера (если не указана)
- Материал (если не указан)

**Вопросы о симптомах:**
- Подробное описание проблемы
- Когда возникает проблема (первый слой, весь объект, etc.)
- Визуальные признаки

**Вопросы о настройках:**
- Текущие параметры печати
- Используемые настройки слайсера

#### 3. Сохранение контекста

- Ответы пользователя сохраняются в `session_state`
- Используются в следующих запросах
- Можно очистить через кнопку в боковой панели

#### 4. Адаптивный диалог

**Если информации достаточно:**
- Система дает конкретные рекомендации
- Высокая уверенность (> 0.7)

**Если информации недостаточно:**
- Система задает уточняющие вопросы
- Низкая уверенность (< 0.7)
- После уточнений - повторный поиск

---

## 📊 Примеры использования

### Пример 1: Добавление статьи (админ)

1. Открыть `http://localhost:8501`
2. Ввести:
   - URL: `https://3dtoday.ru/...`
   - Заголовок: "Как устранить stringing на Ender-3"
   - Содержимое: [текст статьи]
3. Нажать "Проверить и добавить"
4. Проверить результаты валидации
5. Нажать "Добавить статью в KB"

### Пример 2: Диагностика проблемы (пользователь)

1. Открыть `http://localhost:8502`
2. В боковой панели указать:
   - Принтер: Ender-3
   - Материал: PLA
3. Ввести запрос: "У меня ниточки между деталями"
4. Получить рекомендации
5. Если нужны уточнения - ответить на вопросы

---

## 🔧 Настройка

### Изменение портов

**FastAPI:**
```python
# В backend/app/main.py
uvicorn.run(app, host="0.0.0.0", port=8000)
```

**Streamlit:**
```bash
streamlit run frontend/admin_ui.py --server.port 8501
streamlit run frontend/user_ui.py --server.port 8502
```

### Изменение API URL

В обоих интерфейсах:
```python
API_BASE_URL = "http://localhost:8000"  # Изменить при необходимости
```

---

## ✅ Чек-лист запуска

- [ ] Запущен FastAPI: `uvicorn backend.app.main:app --reload`
- [ ] Запущен Qdrant: `./scripts/start_qdrant.sh`
- [ ] Запущен Ollama (если используется): `ollama serve`
- [ ] Запущен административный интерфейс: `streamlit run frontend/admin_ui.py`
- [ ] Запущен пользовательский интерфейс: `streamlit run frontend/user_ui.py --server.port 8502`

---

## 📝 Примечания

- Оба интерфейса работают через единый FastAPI backend
- Административный интерфейс требует запущенного API
- Пользовательский интерфейс требует запущенного API и KB с данными
- История диалога сохраняется в session_state Streamlit
- Контекст пользователя (принтер, материал) сохраняется между запросами


