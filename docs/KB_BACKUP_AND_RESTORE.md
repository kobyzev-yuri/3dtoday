# Резервное копирование и восстановление базы знаний (KB)

## 📋 Обзор

База знаний хранится в векторной БД Qdrant. Для резервного копирования и восстановления KB используются скрипты экспорта/импорта.

## 🔄 Процесс работы с KB

### 1. Экспорт KB (резервное копирование)

**Когда делать:**
- После каждого значимого изменения KB (добавление статей, обновление данных)
- Перед обновлением системы
- Регулярно (например, раз в неделю)

**Как экспортировать:**

```bash
# Экспорт в директорию по умолчанию (knowledge_base/export)
python scripts/export_kb.py

# Или указать свою директорию
python scripts/export_kb.py knowledge_base/backups/2025-12-16
```

**Что создается:**
- `articles_YYYYMMDD_HHMMSS.json` - все текстовые статьи
- `images_YYYYMMDD_HHMMSS.json` - все изображения
- `export_metadata_YYYYMMDD_HHMMSS.json` - метаданные экспорта

**Автоматический коммит в Git:**

После экспорта рекомендуется закоммитить файлы в Git:

```bash
# Экспорт KB
python scripts/export_kb.py

# Добавление файлов экспорта в Git
git add knowledge_base/export/*.json

# Коммит с описанием
git commit -m "KB backup: $(date +%Y-%m-%d) - $(wc -l < knowledge_base/export/articles_*.json | xargs) articles"

# Отправка в GitHub
git push origin main
```

### 2. Импорт KB (восстановление)

**Когда делать:**
- При развертывании системы на новом сервере
- После сбоя или потери данных
- Для восстановления из резервной копии

**Как импортировать:**

```bash
# Импорт из JSON файлов
python scripts/import_kb.py knowledge_base/export/articles_20251216_120000.json
```

**Примечание:** Скрипт импорта создается автоматически при необходимости.

## 🚀 Начальная настройка KB

### Вариант 1: С нуля (пустая KB)

1. **Запустите Qdrant:**
   ```bash
   ./scripts/start_qdrant.sh
   # или
   docker-compose up -d qdrant
   ```

2. **Проверьте подключение:**
   ```bash
   curl http://localhost:6333/health
   ```

3. **Запустите FastAPI:**
   ```bash
   PYTHONPATH=. uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
   ```

4. **Коллекции создадутся автоматически** при первом обращении к API.

5. **Начните добавлять статьи** через административный интерфейс:
   ```bash
   streamlit run frontend/admin_ui.py
   ```

### Вариант 2: С начальной KB из Git

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd 3dtoday
   ```

2. **Найдите последний экспорт KB:**
   ```bash
   ls -lt knowledge_base/export/articles_*.json | head -1
   ```

3. **Запустите Qdrant:**
   ```bash
   ./scripts/start_qdrant.sh
   ```

4. **Импортируйте KB:**
   ```bash
   python scripts/import_kb.py knowledge_base/export/articles_YYYYMMDD_HHMMSS.json
   ```

5. **Проверьте импорт:**
   ```bash
   curl http://localhost:8000/api/kb/statistics
   ```

## 📦 Структура экспорта

### Формат файла статей (articles_*.json)

```json
[
  {
    "id": "123",
    "title": "Как устранить stringing",
    "content": "Полный текст статьи...",
    "url": "https://3dtoday.ru/...",
    "section": "Техничка",
    "problem_type": "stringing",
    "printer_models": ["Ender-3"],
    "materials": ["PLA"],
    "symptoms": ["ниточки"],
    "solutions": [
      {
        "parameter": "retraction_length",
        "value": 6,
        "unit": "mm"
      }
    ],
    "abstract": "Краткое изложение...",
    "relevance_score": 0.9,
    "quality_score": 0.85,
    "content_type": "article",
    "tags": ["stringing", "retraction"],
    "date": "2025-12-16",
    "author": "Автор",
    "images": []
  }
]
```

### Формат файла изображений (images_*.json)

```json
[
  {
    "id": "456",
    "url": "https://example.com/image.jpg",
    "alt": "Stringing example",
    "description": "Пример stringing на PLA",
    "article_id": "123",
    "article_title": "Как устранить stringing",
    "relevance_score": 0.9,
    "problem_type": "stringing",
    "tags": ["stringing", "pla"]
  }
]
```

## 🔧 Поддержка KB

### Регулярные задачи

1. **Еженедельный экспорт:**
   ```bash
   # Добавьте в crontab
   0 2 * * 0 cd /path/to/3dtoday && python scripts/export_kb.py && git add knowledge_base/export/*.json && git commit -m "Weekly KB backup" && git push
   ```

2. **Проверка статистики:**
   ```bash
   curl http://localhost:8000/api/kb/statistics
   ```

3. **Очистка старых экспортов:**
   ```bash
   # Удалить экспорты старше 30 дней
   find knowledge_base/export -name "*.json" -mtime +30 -delete
   ```

### Добавление новых статей

См. [KB_MANAGEMENT.md](KB_MANAGEMENT.md) для подробной инструкции.

### Обновление существующих статей

1. Найдите статью через административный интерфейс
2. Отредактируйте метаданные
3. Сохраните изменения
4. Экспортируйте KB для резервной копии

## 📝 Рекомендации

1. **Регулярные экспорты:** Делайте экспорт после каждого значимого изменения
2. **Версионирование:** Коммитьте экспорты в Git для истории изменений
3. **Проверка:** После импорта всегда проверяйте статистику KB
4. **Резервные копии:** Храните несколько версий экспортов (последние 3-5)

## 🔗 Связанные документы

- [KB_MANAGEMENT.md](KB_MANAGEMENT.md) - Управление KB
- [KB_CREATION_FROM_SCRATCH.md](KB_CREATION_FROM_SCRATCH.md) - Создание KB с нуля
- [INSTALLATION.md](INSTALLATION.md) - Установка системы

