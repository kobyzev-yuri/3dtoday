# Процесс ручного сбора статей для KB

## 📋 Обзор

Процесс ручного сбора статей включает:
1. **Отбор статьи** из 3dtoday.ru
2. **Валидация релевантности** через автоматическую проверку
3. **Извлечение метаданных** через LLM
4. **Индексация** в Qdrant

## 🛠️ Инструменты

### 1. CLI инструмент (`article_collector.py`)

Интерактивный инструмент для командной строки.

**Запуск:**
```bash
python3 backend/app/tools/article_collector.py
```

**Процесс:**
1. Ввод URL статьи (опционально)
2. Ввод заголовка
3. Ввод содержимого (завершить вводом 'END')
4. Автоматическая валидация релевантности
5. Извлечение метаданных
6. Подтверждение и индексация

### 2. Streamlit интерфейс (`article_collector_ui.py`)

Веб-интерфейс для удобного добавления статей.

**Запуск:**
```bash
streamlit run frontend/article_collector_ui.py
```

**Возможности:**
- Удобная форма ввода
- Визуализация результатов валидации
- Отображение извлеченных метаданных
- Интерактивное подтверждение

## 🔍 Проверка релевантности

### Автоматическая проверка через LLM

Система проверяет:

1. **Релевантность (relevance_score)**
   - Содержит ли статья информацию о проблемах 3D-печати?
   - Есть ли конкретные решения?
   - Упоминаются ли модели принтеров, материалы, параметры?
   - Полезна ли информация для диагностики?

2. **Качество (quality_score)**
   - Структурированность
   - Конкретность (параметры, значения)
   - Полнота информации
   - Актуальность

3. **Наличие решений (has_solutions)**
   - Есть ли конкретные решения с параметрами?
   - Упоминаются ли значения (температура, скорость, retraction)?

### Критерии принятия

Статья принимается, если:
- ✅ `relevance_score >= 0.7`
- ✅ `quality_score >= 0.6`
- ✅ `has_solutions == True`

### Fallback проверка

Если LLM недоступен, используется простая проверка по ключевым словам:
- Ключевые слова проблем (stringing, warping, layer, etc.)
- Ключевые слова решений (температура, скорость, retraction, etc.)
- Ключевые слова оборудования (принтер, PLA, PETG, etc.)

## 📝 Извлечение метаданных

### Автоматическое извлечение через LLM

Система извлекает:

1. **problem_type** - тип проблемы:
   - stringing
   - warping
   - layer_separation
   - bed_adhesion
   - overhang
   - underextrusion
   - overextrusion
   - или null

2. **printer_models** - модели принтеров:
   - ["Ender-3", "Anycubic Kobra", ...]
   - или []

3. **materials** - материалы:
   - ["PLA", "PETG", "ABS", ...]
   - или []

4. **symptoms** - симптомы:
   - ["ниточки", "отслоение", ...]
   - или []

5. **solutions** - решения с параметрами:
   ```json
   [
     {
       "parameter": "retraction_length",
       "value": 6,
       "unit": "mm",
       "description": "Увеличьте retraction до 6 мм"
     }
   ]
   ```

### Валидация метаданных

- Проверка типов данных
- Проверка обязательных полей
- Валидация формата решений

## 💾 Индексация

После успешной валидации и извлечения метаданных:

1. **Генерация article_id**
   - Формат: `{problem_type}_{hash(title)}`

2. **Подготовка данных**
   - Объединение валидации и метаданных
   - Форматирование для Qdrant

3. **Генерация эмбеддингов**
   - Автоматическая генерация через Sentence Transformers
   - Размерность: 768 dim

4. **Добавление в Qdrant**
   - Индексация в коллекцию `kb_3dtoday`
   - Сохранение метаданных в payload

## 📊 Пример процесса

### Шаг 1: Отбор статьи

Находим статью на 3dtoday.ru:
- URL: `https://3dtoday.ru/...`
- Раздел: "Техничка"
- Тема: "Как устранить stringing на Ender-3"

### Шаг 2: Валидация

```python
validation = await collector.validate_article_relevance(
    title="Как устранить stringing на Ender-3",
    content="Полный текст статьи...",
    url="https://3dtoday.ru/..."
)

# Результат:
# {
#   "relevance_score": 0.85,
#   "quality_score": 0.78,
#   "has_solutions": True,
#   "is_relevant": True
# }
```

### Шаг 3: Извлечение метаданных

```python
metadata = await collector.extract_metadata(title, content)

# Результат:
# {
#   "problem_type": "stringing",
#   "printer_models": ["Ender-3"],
#   "materials": ["PLA"],
#   "symptoms": ["ниточки", "сопли"],
#   "solutions": [
#     {
#       "parameter": "retraction_length",
#       "value": 6,
#       "unit": "mm",
#       "description": "Увеличьте retraction до 6 мм"
#     }
#   ]
# }
```

### Шаг 4: Индексация

```python
result = await collector.process_and_index_article(
    title, content, url, section="Техничка"
)

# Результат:
# {
#   "success": True,
#   "article_id": "stringing_1234",
#   "validation": {...},
#   "metadata": {...}
# }
```

## ✅ Критерии качества статьи

### Хорошая статья:

✅ Содержит конкретные решения с параметрами
✅ Упоминает модели принтеров и материалы
✅ Имеет четкую структуру (проблема → решение)
✅ Актуальная информация
✅ Релевантность >= 0.7

### Плохая статья (не добавлять):

❌ Общие рассуждения без конкретики
❌ Устаревшая информация
❌ Нет конкретных решений
❌ Релевантность < 0.7

## 🔄 Ручная проверка (опционально)

После автоматической валидации можно проверить вручную:

1. **Проверка релевантности**
   - Соответствует ли статья теме проекта?
   - Есть ли конкретные решения?

2. **Проверка метаданных**
   - Правильно ли определен problem_type?
   - Корректны ли printer_models и materials?
   - Полны ли solutions?

3. **Проверка качества**
   - Структурированность
   - Конкретность
   - Актуальность

## 📝 Рекомендации

1. **Начинать с простых статей**
   - Одна проблема на статью
   - Четкие решения
   - Хорошо структурированные

2. **Проверять метаданные**
   - Автоматическое извлечение может быть неточным
   - Ручная проверка и корректировка при необходимости

3. **Тестировать поиск**
   - После индексации проверить поиск
   - Убедиться, что статья находится по релевантным запросам

4. **Вести журнал**
   - Записывать добавленные статьи
   - Отмечать проблемы и улучшения

## 🚀 Быстрый старт

### Использование Streamlit интерфейса:

1. Запустить интерфейс:
   ```bash
   streamlit run frontend/article_collector_ui.py
   ```

2. Открыть браузер: `http://localhost:8501`

3. Заполнить форму:
   - URL статьи
   - Заголовок
   - Содержимое

4. Нажать "Проверить и добавить в KB"

5. Проверить результаты валидации

6. Подтвердить добавление

### Использование CLI инструмента:

1. Запустить инструмент:
   ```bash
   python3 backend/app/tools/article_collector.py
   ```

2. Следовать инструкциям в интерактивном режиме

## 📚 Дополнительные ресурсы

- `KB_MAINTENANCE.md` - руководство по поддержке KB
- `KB_CURATOR_AGENT.md` - описание KB Curator Agent
- `PARSING_AND_TESTING_STRATEGY.md` - стратегия парсинга


