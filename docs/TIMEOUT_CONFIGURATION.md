# Конфигурация таймаутов

## ⏱️ Настройки таймаутов в config.env

Все таймауты настраиваются в `config.env` (в секундах):

### HTTP и парсинг
- `HTTP_TIMEOUT=30` - общий таймаут для HTTP запросов
- `PARSER_TIMEOUT=30` - общий таймаут для парсеров (fallback)
- `ARTICLE_PARSER_TIMEOUT=30` - таймаут для парсера статей
- `QUESTIONS_PARSER_TIMEOUT=30` - таймаут для парсера вопросов
- `DOCUMENT_PARSER_TIMEOUT=30` - таймаут для универсального парсера документов

### LLM провайдеры
- `OLLAMA_TIMEOUT=500` - таймаут для Ollama (большой, т.к. локальные модели медленные)
- `OPENAI_TIMEOUT=60` - таймаут для OpenAI/ProxyAPI
- `OLLAMA_VISION_TIMEOUT=300` - таймаут для анализа изображений через Ollama

### Системные таймауты
- `MCP_SERVER_TIMEOUT=180` - таймаут для MCP сервера (парсинг документов)
- `API_REQUEST_TIMEOUT=120` - таймаут для API запросов из интерфейса
- `RAG_SEARCH_TIMEOUT=30` - таймаут для RAG поиска
- `HEALTH_CHECK_TIMEOUT=10` - таймаут для проверки здоровья сервисов

---

## 🔧 Использование в коде

### Парсеры документов
```python
self.timeout = float(os.getenv("DOCUMENT_PARSER_TIMEOUT", os.getenv("PARSER_TIMEOUT", "30")))
```

### LLM клиент
```python
self.timeout = int(os.getenv("OLLAMA_TIMEOUT", "500"))  # Ollama
self.timeout = int(os.getenv("OPENAI_TIMEOUT", "60"))   # OpenAI
```

### MCP сервер
```python
mcp_timeout = int(os.getenv("MCP_SERVER_TIMEOUT", "180"))
```

---

## 🖥️ Интерфейс администратора

В интерфейсе администратора (`frontend/admin_ui.py`) доступны:

### Выбор LLM провайдера и модели
- **OpenAI**: GPT-4o, GPT-4-turbo, GPT-3.5-turbo
- **Ollama**: любая модель (по умолчанию llama3.1:70b)
- **Gemini**: gemini-3-pro-preview, gemini-pro, gemini-1.5-pro

### Настройка таймаутов
Все таймауты отображаются в боковой панели и могут быть изменены:
- API запросы (по умолчанию 120 сек)
- Парсинг документов (по умолчанию 30 сек)
- LLM генерация Ollama (по умолчанию 500 сек)
- LLM генерация OpenAI (по умолчанию 60 сек)
- MCP сервер (по умолчанию 180 сек)
- RAG поиск (по умолчанию 30 сек)
- Health check (по умолчанию 10 сек)

---

## 📋 Рекомендации по таймаутам

### Для быстрых операций
- Парсинг HTML: 30 сек
- RAG поиск: 30 сек
- Health check: 10 сек

### Для медленных операций
- Ollama генерация: 500 сек (локальные модели медленные)
- MCP парсинг документов: 180 сек (может включать анализ через LLM)
- API запросы: 120 сек (включают полный цикл парсинга + анализа)

### Для облачных API
- OpenAI/ProxyAPI: 60 сек (быстрые облачные модели)
- Gemini: 60 сек

---

## ⚠️ Решение проблем с таймаутами

### Ошибка: "Timeout error"
1. Увеличьте соответствующий таймаут в `config.env`
2. Или измените таймаут в интерфейсе администратора
3. Для Ollama: убедитесь, что модель загружена (`ollama pull llama3.1:70b`)

### Ошибка: "Connection timeout"
1. Проверьте доступность сервисов (Qdrant, Ollama)
2. Увеличьте `HEALTH_CHECK_TIMEOUT`
3. Проверьте сетевые настройки

### Медленная работа
1. Для парсинга: увеличьте `DOCUMENT_PARSER_TIMEOUT`
2. Для LLM: увеличьте `OLLAMA_TIMEOUT` или `OPENAI_TIMEOUT`
3. Для MCP: увеличьте `MCP_SERVER_TIMEOUT`

---

## 📚 Дополнительная информация

- `config.env` - файл конфигурации
- `config.env.example` - пример конфигурации
- `docs/README_CONFIG.md` - документация по конфигурации
- `frontend/admin_ui.py` - интерфейс администратора с настройками

