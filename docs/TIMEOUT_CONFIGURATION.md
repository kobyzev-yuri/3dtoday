# Конфигурация таймаутов

## ⏱️ Настройки таймаутов в config.env

Все таймауты настраиваются в `config.env` (в секундах):

### HTTP и парсинг
- `HTTP_TIMEOUT=30` - общий таймаут для HTTP запросов
- `PARSER_TIMEOUT=30` - общий таймаут для парсеров (fallback)
- `ARTICLE_PARSER_TIMEOUT=30` - таймаут для парсера статей
- `QUESTIONS_PARSER_TIMEOUT=30` - таймаут для парсера вопросов
- `DOCUMENT_PARSER_TIMEOUT=30` - таймаут для универсального парсера документов

### LLM провайдеры
- `OLLAMA_TIMEOUT=100` - таймаут для Ollama по умолчанию
- `OLLAMA_TIMEOUT_LIGHT=100` - таймаут для легкой модели Ollama (qwen2.5:1.5b)
- `OLLAMA_TIMEOUT_HEAVY=900` - таймаут для тяжелой модели Ollama (qwen3:8b)
- `OPENAI_TIMEOUT=600` - таймаут для OpenAI/ProxyAPI
- `GEMINI_TIMEOUT=600` - таймаут для Gemini/ProxyAPI
- `OLLAMA_VISION_TIMEOUT=600` - таймаут для анализа изображений через Ollama

### Системные таймауты
- `MCP_SERVER_TIMEOUT=180` - таймаут для MCP сервера (парсинг документов)
- `API_REQUEST_TIMEOUT=120` - таймаут для API запросов из интерфейса
- `RAG_SEARCH_TIMEOUT=30` - таймаут для RAG поиска
- `HEALTH_CHECK_TIMEOUT=10` - таймаут для проверки здоровья сервисов

---

## 🔧 Использование в коде

### Парсеры документов
```python
self.timeout = float(os.getenv("DOCUMENT_PARSER_TIMEOUT", os.getenv("PARSER_TIMEOUT", "30")))
```

### LLM клиент
```python
# Таймаут передается динамически из запроса или из переменных окружения
self.timeout = int(os.getenv("OLLAMA_TIMEOUT", "100"))  # Ollama (по умолчанию легкая модель)
self.timeout = int(os.getenv("OPENAI_TIMEOUT", "600"))  # OpenAI
self.timeout = int(os.getenv("GEMINI_TIMEOUT", "600"))  # Gemini
```

### MCP сервер
```python
mcp_timeout = int(os.getenv("MCP_SERVER_TIMEOUT", "180"))
```

---

## 🖥️ Интерфейс администратора

В интерфейсе администратора (`frontend/admin_ui.py`) доступны:

### Выбор LLM провайдера и модели
- **OpenAI**: GPT-4o, GPT-4-turbo, GPT-3.5-turbo
- **Ollama**: любая модель (по умолчанию llama3.1:70b)
- **Gemini**: gemini-3-pro-preview, gemini-pro, gemini-1.5-pro

### Настройка таймаутов
Все таймауты отображаются в боковой панели и могут быть изменены:
- Диагностика (общий) (по умолчанию 300 сек)
- LLM генерация Ollama (по умолчанию 100 сек для легкой, 900 сек для тяжелой)
- LLM генерация OpenAI (по умолчанию 600 сек)
- LLM генерация Gemini (по умолчанию 600 сек)
- RAG поиск (по умолчанию 30 сек)
- API запросы (по умолчанию 300 сек)

---

## 📋 Рекомендации по таймаутам

### Для быстрых операций
- Парсинг HTML: 30 сек
- RAG поиск: 30 сек
- Health check: 10 сек

### Для медленных операций
- Ollama легкая модель (qwen2.5:1.5b): 100 сек (~86 сек фактически)
- Ollama тяжелая модель (qwen3:8b): 900 сек (>500 сек фактически)
- MCP парсинг документов: 300 сек (может включать анализ через LLM)
- API запросы: 300 сек (включают полный цикл парсинга + анализа)

### Для облачных API
- OpenAI/ProxyAPI: 600 сек (быстрые облачные модели, но с запасом)
- Gemini: 600 сек (быстрые облачные модели, но с запасом)

---

## ⚠️ Решение проблем с таймаутами

### Ошибка: "Timeout error"
1. Увеличьте соответствующий таймаут в `config.env` или в пользовательском интерфейсе
2. Для Ollama легкой модели (qwen2.5:1.5b): минимум 100 сек
3. Для Ollama тяжелой модели (qwen3:8b): минимум 900 сек
4. Убедитесь, что модель загружена (`ollama pull qwen2.5:1.5b` или `ollama pull qwen3:8b`)
5. Если модель слишком медленная, используйте более легкую модель

### Ошибка: "Connection timeout"
1. Проверьте доступность сервисов (Qdrant, Ollama)
2. Увеличьте `HEALTH_CHECK_TIMEOUT`
3. Проверьте сетевые настройки

### Медленная работа
1. Для парсинга: увеличьте `DOCUMENT_PARSER_TIMEOUT`
2. Для LLM: увеличьте `OLLAMA_TIMEOUT` или `OPENAI_TIMEOUT`
3. Для MCP: увеличьте `MCP_SERVER_TIMEOUT`

---

## 📚 Дополнительная информация

- `config.env` - файл конфигурации
- `config.env.example` - пример конфигурации
- `docs/README_CONFIG.md` - документация по конфигурации
- `frontend/admin_ui.py` - интерфейс администратора с настройками


