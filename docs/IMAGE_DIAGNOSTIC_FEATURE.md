# Функция диагностики с изображениями

## 📸 Описание

Добавлена возможность диагностики проблем 3D-печати с анализом изображений дефектов через пользовательский интерфейс.

## ✨ Возможности

1. **Загрузка изображения** - пользователь может загрузить фото дефекта
2. **Автоматический анализ** - изображение анализируется через Vision Analyzer (Gemini/Ollama)
3. **Улучшенный поиск** - RetrievalAgent использует контекст изображения для более точного поиска
4. **Реранкинг результатов** - результаты переранжируются с учетом контекста изображения

## 🔧 Технические детали

### Backend (`backend/app/main.py`)

**Endpoint:** `POST /api/diagnose/image`

**Параметры:**
- `query` (str) - текстовый запрос
- `printer_model` (optional) - модель принтера
- `material` (optional) - материал
- `problem_type` (optional) - тип проблемы
- `image` (file) - изображение дефекта
- `use_reranking` (bool, default=True) - использовать реранкинг
- `limit` (int, default=5) - количество результатов

**Использует:**
- `RetrievalAgent` для поиска с анализом изображения
- `VisionAnalyzer` для анализа изображения (Gemini/Ollama)
- Cross-Encoder для реранкинга результатов

### Frontend (`frontend/user_ui.py`)

**Изменения:**
1. Добавлен предпросмотр загруженного изображения
2. Автоматическое определение использования изображения
3. Вызов `/api/diagnose/image` при наличии изображения
4. Отображение информации об анализе изображения
5. Улучшенное отображение результатов с метаданными

## 🚀 Использование

### В пользовательском интерфейсе:

1. Откройте пользовательский интерфейс
2. Введите текстовый запрос (опционально)
3. Загрузите изображение дефекта через `st.file_uploader`
4. Нажмите "🔍 Получить диагностику"
5. Система автоматически:
   - Проанализирует изображение
   - Извлечет контекст (problem_type, symptoms)
   - Выполнит поиск с учетом контекста
   - Применит реранкинг для улучшения результатов

### Пример запроса через API:

```bash
curl -X POST "http://localhost:8000/api/diagnose/image" \
  -F "query=Что не так с печатью?" \
  -F "printer_model=Ender-3" \
  -F "material=PLA" \
  -F "image=@stringing_example.jpg" \
  -F "use_reranking=true" \
  -F "limit=5"
```

## 📊 Результаты

Ответ содержит:
- `success` - статус успешности
- `answer` - **человеческий ответ, сгенерированный через LLM** на основе найденных статей
- `query` - исходный запрос
- `image_name` - имя изображения
- `image_size` - размер изображения
- `relevant_articles` - список найденных статей с метаданными (используются как источники):
  - `title` - заголовок статьи
  - `content` - содержание
  - `score` - релевантность (0-1)
  - `rerank_score` - score после реранкинга
  - `problem_type` - тип проблемы
  - `printer_models` - модели принтеров
  - `materials` - материалы
  - `url` - ссылка на статью
- `results_count` - количество найденных статей
- `confidence` - уверенность в ответе (0.0-1.0)
- `needs_clarification` - нужны ли уточнения
- `clarification_questions` - список уточняющих вопросов (если нужны)
- `image_analysis` - флаг успешного анализа изображения

## 🔍 Как это работает

1. **Анализ изображения:**
   - Vision Analyzer анализирует изображение через Gemini или Ollama/llava
   - Извлекается текстовое описание дефекта
   - Определяется `problem_type` и `symptoms` из текста

2. **Улучшение запроса:**
   - Текстовый запрос дополняется описанием из изображения
   - Фильтры обновляются с учетом извлеченного `problem_type`

3. **Поиск в KB:**
   - Выполняется гибридный поиск (векторный + фильтры)
   - Получается больше кандидатов для реранкинга

4. **Реранкинг:**
   - Cross-Encoder переоценивает пары (запрос, статья)
   - Результаты сортируются по комбинированному score

5. **Генерация ответа через LLM:**
   - Найденные статьи используются как контекст для LLM
   - LLM генерирует человеческий, эмпатичный ответ
   - Ответ структурирован: проблема → решение → параметры
   - Статьи предоставляются как источники, а не просто ссылки

## ⚙️ Настройки

### Vision Analyzer

В `config.env`:
```bash
# Gemini (основной)
GEMINI_API_KEY=your_key
GEMINI_MODEL=gemini-3-pro-preview
GEMINI_BASE_URL=https://api.proxyapi.ru/google

# Ollama (fallback)
OLLAMA_VISION_MODEL=llava
OLLAMA_VISION_TIMEOUT=600
```

### RetrievalAgent

Автоматически использует:
- Cross-Encoder модель: `cross-encoder/ms-marco-MiniLM-L-12-v2`
- Комбинирование scores: 40% оригинальный + 60% rerank

## 🧪 Тестирование

Запустите тест:
```bash
python tools/test_retrieval_agent.py
```

Тесты включают:
1. Поиск только по тексту
2. Поиск с контекстом изображения (vision_context)
3. Поиск с анализом изображения из файла
4. Поиск с анализом изображения из байтов

## 📝 Примечания

- Если Gemini недоступен, автоматически используется Ollama/llava
- Реранкинг можно отключить установкой `use_reranking=false`
- Изображения оптимизируются автоматически для ускорения анализа

