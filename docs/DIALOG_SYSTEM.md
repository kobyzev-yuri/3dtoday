# Система диалога с пользователем

## 🎯 Механизм получения уточнений

### Автоматическое определение необходимости уточнений

Система проверяет:

1. **Контекст пользователя:**
   - Есть ли модель принтера?
   - Есть ли материал?
   - Есть ли тип проблемы?

2. **Качество поиска:**
   - Количество найденных статей
   - Релевантность результатов (score)
   - Соответствие фильтрам

3. **Достаточность информации:**
   - Можно ли дать конкретный ответ?
   - Нужны ли дополнительные детали?

---

## 💬 Типы уточняющих вопросов

### 1. Вопросы о контексте

**Модель принтера:**
- Вопрос: "Какая у вас модель принтера?"
- Тип: `printer_model`
- Варианты: можно предложить список популярных моделей

**Материал:**
- Вопрос: "Какой материал вы используете?"
- Тип: `material`
- Варианты: ["PLA", "PETG", "ABS", "TPU", "Другое"]

### 2. Вопросы о симптомах

**Подробное описание:**
- Вопрос: "Можете описать проблему подробнее? Что именно происходит?"
- Тип: `symptom`
- Цель: получить больше деталей для точной диагностики

**Визуальные признаки:**
- Вопрос: "Что вы видите на печати? (ниточки, трещины, отслоение)"
- Тип: `visual_symptom`
- Варианты: можно предложить список типичных симптомов

### 3. Вопросы о настройках

**Текущие параметры:**
- Вопрос: "Какая у вас температура сопла и стола?"
- Тип: `settings`
- Цель: сравнить с рекомендуемыми значениями

**Этап печати:**
- Вопрос: "Когда возникает проблема? (первый слой, весь объект, углы)"
- Тип: `print_stage`
- Варианты: ["первый_слой", "весь_объект", "углы", "верхние_слои"]

---

## 🔄 Процесс диалога

### Пример 1: Минимальный контекст

```
Пользователь: "У меня проблема с печатью"

Система проверяет:
- ❌ Нет модели принтера
- ❌ Нет материала
- ❌ Нет описания проблемы

Система задает вопросы:
1. "Какая у вас модель принтера?"
2. "Какой материал вы используете?"
3. "Опишите проблему подробнее"

Пользователь отвечает:
- Принтер: Ender-3
- Материал: PLA
- Проблема: "Ниточки между деталями"

Система:
- Сохраняет контекст
- Ищет в KB: "stringing Ender-3 PLA"
- Дает конкретные рекомендации
```

### Пример 2: Частичный контекст

```
Пользователь: "У меня ниточки при печати PLA"

Система проверяет:
- ❌ Нет модели принтера
- ✅ Есть материал (PLA)
- ✅ Есть описание (ниточки → stringing)

Система задает вопрос:
1. "Какая у вас модель принтера?"

Пользователь отвечает:
- Принтер: Ender-3

Система:
- Сохраняет контекст
- Ищет в KB: "stringing Ender-3 PLA"
- Дает конкретные рекомендации
```

### Пример 3: Полный контекст

```
Пользователь: "У меня stringing на Ender-3 с PLA"

Система проверяет:
- ✅ Есть модель принтера (Ender-3)
- ✅ Есть материал (PLA)
- ✅ Есть тип проблемы (stringing)

Система:
- Ищет в KB: "stringing Ender-3 PLA"
- Находит релевантные статьи
- Дает конкретные рекомендации БЕЗ уточнений
```

---

## 📊 Варианты ввода информации о статьях

### Вариант 1: Ручной ввод через интерфейс

**Процесс:**
1. Найти статью на 3dtoday.ru
2. Скопировать текст
3. Вставить в форму административного интерфейса
4. Система автоматически:
   - Валидирует релевантность
   - Извлекает метаданные
   - Проверяет качество

**Преимущества:**
- ✅ Простота использования
- ✅ Визуальная обратная связь
- ✅ Автоматическая валидация

### Вариант 2: Импорт из JSON

**Формат:**
```json
{
  "title": "Как устранить stringing",
  "content": "Полный текст...",
  "url": "https://3dtoday.ru/...",
  "section": "Техничка",
  "metadata": {
    "problem_type": "stringing",
    "printer_models": ["Ender-3"],
    "materials": ["PLA"],
    "solutions": [
      {
        "parameter": "retraction_length",
        "value": 6,
        "unit": "mm"
      }
    ]
  }
}
```

**Использование:**
- Через API: `POST /api/kb/articles/add`
- Можно создать скрипт для массового импорта

### Вариант 3: CLI инструмент

**Запуск:**
```bash
python3 backend/app/tools/article_collector.py
```

**Процесс:**
- Интерактивный ввод
- Валидация
- Индексация

---

## 🎯 Стратегия диалога

### Уровень 1: Сбор базового контекста

**Цель:** Получить минимально необходимую информацию

**Вопросы:**
1. Модель принтера (если не указана)
2. Материал (если не указан)

**Когда задавать:**
- При первом запросе пользователя
- Если контекст не заполнен

### Уровень 2: Уточнение симптомов

**Цель:** Получить больше деталей о проблеме

**Вопросы:**
1. Подробное описание проблемы
2. Визуальные признаки
3. Когда возникает проблема

**Когда задавать:**
- Если поиск дает мало результатов
- Если релевантность низкая (< 0.7)
- Если проблема неоднозначна

### Уровень 3: Уточнение настроек

**Цель:** Сравнить с рекомендуемыми параметрами

**Вопросы:**
1. Текущие параметры печати
2. Настройки слайсера
3. Используемые аксессуары (клей, PEI, etc.)

**Когда задавать:**
- Если стандартные решения не помогли
- Если нужна более точная диагностика

---

## 💡 Примеры диалогов

### Диалог 1: Постепенное уточнение

```
Пользователь: "У меня проблема"

Система: "Для точной диагностики нужны уточнения:
1. Какая у вас модель принтера?
2. Какой материал вы используете?
3. Опишите проблему подробнее"

Пользователь: "Ender-3, PLA, ниточки"

Система: "Похоже на stringing. Рекомендации:
- Retraction: 6 мм
- Скорость retraction: 45 мм/с
- Температура: 200°C"
```

### Диалог 2: Итеративное уточнение

```
Пользователь: "Печать отслаивается"

Система: "Нужны уточнения:
1. Модель принтера?
2. Материал?"

Пользователь: "Ender-3, PETG"

Система: "Похоже на warping. Рекомендации:
- Температура стола: 80°C
- Отключить вентилятор на первых слоях
- Использовать brim"

Пользователь: "Попробовал, все равно отслаивается"

Система: "Нужны дополнительные уточнения:
1. Стол чистый? (используйте изопропиловый спирт)
2. Используете ли клей или PEI?
3. Есть ли сквозняки?"
```

---

## 🔧 Реализация в коде

### Определение необходимости уточнений

```python
needs_clarification = False
clarification_questions = []

# Проверка контекста
if not request.printer_model:
    needs_clarification = True
    clarification_questions.append(
        ClarificationQuestion(
            question="Какая у вас модель принтера?",
            question_type="printer_model"
        )
    )

if not request.material:
    needs_clarification = True
    clarification_questions.append(
        ClarificationQuestion(
            question="Какой материал вы используете?",
            question_type="material",
            options=["PLA", "PETG", "ABS", "TPU"]
        )
    )

# Проверка качества поиска
if search_results and len(search_results) < 2:
    if search_results[0].get("score", 0) < 0.7:
        needs_clarification = True
        clarification_questions.append(
            ClarificationQuestion(
                question="Опишите проблему подробнее",
                question_type="symptom"
            )
        )
```

### Сохранение контекста

```python
# В Streamlit session_state
st.session_state.user_context = {
    "printer_model": "Ender-3",
    "material": "PLA",
    "problem_type": "stringing"
}

# Использование в следующем запросе
request = DiagnosticRequest(
    query=query,
    printer_model=st.session_state.user_context.get("printer_model"),
    material=st.session_state.user_context.get("material")
)
```

---

## ✅ Преимущества подхода

1. **Адаптивность** - система подстраивается под уровень детализации пользователя
2. **Точность** - больше контекста → точнее диагностика
3. **Удобство** - пользователь не обязан знать все сразу
4. **Эффективность** - система запрашивает только необходимое

---

## 📚 Дополнительная информация

- `docs/STREAMLIT_INTERFACES_GUIDE.md` - руководство по интерфейсам
- `QUICK_START.md` - быстрый старт



