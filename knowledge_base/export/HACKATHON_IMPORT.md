# 🚀 Быстрый импорт KB для организаторов хакатона

## 📦 Что нужно для импорта

1. **Клонировать репозиторий:**
   ```bash
   git clone https://github.com/kobyzev-yuri/3dtoday.git
   cd 3dtoday
   ```

2. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Запустить Qdrant:**
   ```bash
   ./scripts/start_qdrant.sh
   # Или через Docker:
   docker run -d -p 6333:6333 qdrant/qdrant
   ```

## 📥 Импорт KB

### Простой способ (рекомендуется):

```bash
# Импорт всех статей из экспорта
python scripts/import_kb.py knowledge_base/export/articles_20251217_000422.json
```

**Что произойдет:**
- ✅ Скрипт проверит подключение к Qdrant
- ✅ Автоматически загрузит модель эмбеддингов (при первом запуске)
- ✅ Сгенерирует эмбеддинги для всех 10 статей
- ✅ Индексирует статьи в Qdrant
- ✅ Покажет статистику импорта

**Ожидаемый результат:**
```
✅ Подключение к Qdrant: localhost:6333
✅ ArticleIndexer инициализирован
📚 Загружено статей: 10
🔄 Начало импорта 10 статей...
   ✅ Импортировано: 10/10 статей

✅ Импорт статей завершен!
   📊 Всего: 10
   ✅ Импортировано: 10
   ⏭️  Пропущено: 0
   ❌ Ошибок: 0
```

## ✅ Проверка импорта

После импорта проверьте статистику:

```bash
# Если FastAPI запущен
curl http://localhost:8000/api/kb/statistics

# Или через Qdrant напрямую
curl http://localhost:6333/collections/kb_3dtoday
```

## 📊 Содержимое KB

**Текущий экспорт содержит:**
- 📚 **10 статей** по диагностике проблем 3D-печати
- 🖼️ **2 изображения** дефектов
- 📅 Дата экспорта: 2025-12-17

**Темы статей:**
- Stringing (ниточки между деталями)
- Warping (деформация)
- Layer adhesion (слои не склеиваются)
- И другие проблемы 3D-печати

## 🔧 Требования

- Python 3.8+
- Qdrant (векторная БД)
- Модель эмбеддингов: `intfloat/multilingual-e5-base` (загрузится автоматически)

## ⚠️ Важные замечания

1. **Первый запуск:** Модель эмбеддингов загрузится автоматически (~500MB), это может занять несколько минут
2. **Qdrant должен быть запущен:** Проверьте `curl http://localhost:6333/health`
3. **Память:** Для работы модели эмбеддингов требуется ~2GB RAM

## 📚 Дополнительная документация

- Полная инструкция: `docs/KB_BACKUP_AND_RESTORE.md`
- Быстрый старт: `docs/KB_QUICK_START.md`
- Управление KB: `docs/KB_MANAGEMENT.md`

## 🆘 Проблемы?

**Ошибка подключения к Qdrant:**
```bash
# Проверьте, запущен ли Qdrant
curl http://localhost:6333/health

# Если нет - запустите
./scripts/start_qdrant.sh
```

**Ошибка загрузки модели:**
- Убедитесь, что есть интернет-соединение (для первой загрузки)
- Проверьте свободное место на диске (~1GB)

**Другие проблемы:**
- Проверьте логи скрипта
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

